machine:

  environment:
    PATH: ~/cmake-3.10.0-Linux-x86_64/bin/:~/.local/bin/:$PATH
    LD_LIBRARY_PATH: /home/ubuntu/hasktorch/vendor/build/:/usr/lib/x86_64-linux-gnu/$LD_LIBRARY_PATH
    LLVM_CONFIG: /usr/lib/llvm-3.8/bin/llvm-config

dependencies:

  cache_directories:
    - "/home/ubuntu/.stack"
    - "/home/ubuntu/cmake-3.10.0-Linux-x86_64"
    - "/usr/lib/llvm-4.0"
    - "/home/ubuntu/hasktorch/.stack-work"
    - "/home/ubuntu/hasktorch/vendor/build/"

  pre:
    - wget -O - http://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
    - sudo apt-add-repository "deb http://apt.llvm.org/trusty/ llvm-toolchain-trusty-4.0 main"
    - sudo apt-get update --fix-missing
    - sudo apt-get install clang-4.0 lldb-4.0 --fix-missing
    - >
      if [ ! -d ~/cmake-3.10.0-Linux-x86_64 ]; then
        echo "No cache - building CMake"
        cd ~ && wget --no-check-certificate --quiet https://cmake.org/files/v3.10/cmake-3.10.0-Linux-x86_64.tar.gz && tar -xvf cmake-3.10.0-Linux-x86_64.tar.gz
      else
        echo "Cached CMake found"
      fi
    - mkdir -p ~/.local/bin
    # full path to curl circumvents errors with /usr/local/bin/curl incompatabilities with libcurl
    - /usr/bin/curl -L --retry 3 https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
    - stack upgrade
    - stack update
    - stack install cabal-install --resolver lts-11.2
    - git submodule update --init --recursive
    - alias cmake=~/cmake-3.10.0-Linux-x86_64/bin/cmake
    - sudo pip install pyyaml
    - cd ./vendor; ./build-aten.sh
    - cd ./vendor; ./build-aten-spec.sh
    - cd ./vendor; ./build-error-handler.sh

  override:
    - cabal new-build all

test:
  override:
    ### tests
    - cabal new-test all

    ### examples
    # - cabal new-run hasktorch-examples:static-tensor-usage
    # - cabal new-run hasktorch-examples:ff-typed
    # - cabal new-run hasktorch-examples:ff-untyped
    # - cabal new-run hasktorch-examples:gradient-descent
    # - cabal new-run hasktorch-examples:static-tensor-usage

    ### code generation
    - cabal new-run ht-codegen -- --verbose --type concrete --lib TH
    - cabal new-run ht-codegen -- --verbose --type concrete --lib THC
    - cabal new-run ht-codegen -- --verbose --type generic  --lib TH
    - cabal new-run ht-codegen -- --verbose --type generic  --lib THC
    - cabal new-run ht-codegen -- --verbose --type generic  --lib THNN
    - cabal new-run ht-codegen -- --verbose --type generic  --lib THCUNN

    ### end-to-end build

    # FIXME: make sure these are updated
    # - make init
