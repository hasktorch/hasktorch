-- generated by using spec/Declarations.yaml
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PolyKinds #-}
{-# LANGUAGE QuasiQuotes #-}
{-# LANGUAGE ScopedTypeVariables #-}
{-# LANGUAGE TemplateHaskell #-}

module Torch.Functional.Internal where

import Foreign.ForeignPtr
import System.IO.Unsafe
import Torch.Cast
import Torch.DType
import Torch.Dimname
import Torch.Internal.Cast
import qualified Torch.Internal.Const as ATen
import qualified Torch.Internal.Managed.Cast
import qualified Torch.Internal.Managed.Native as ATen
import qualified Torch.Internal.Managed.Type.Scalar as ATen
import qualified Torch.Internal.Managed.Type.Tensor as ATen
import qualified Torch.Internal.Managed.Type.Tuple as ATen
import qualified Torch.Internal.Type as ATen
import Torch.Scalar
import Torch.Tensor

align_tensors ::
  -- | tensors
  [Tensor] ->
  [Tensor]
align_tensors _tensors = unsafePerformIO $ (cast1 ATen.align_tensors_l) _tensors

native_dropout ::
  -- | input
  Tensor ->
  -- | p
  Double ->
  -- | train
  Bool ->
  (Tensor, Tensor)
native_dropout _input _p _train = unsafePerformIO $ (cast3 ATen.native_dropout_tdb) _input _p _train

dropout ::
  -- | input
  Tensor ->
  -- | p
  Double ->
  -- | train
  Bool ->
  Tensor
dropout _input _p _train = unsafePerformIO $ (cast3 ATen.dropout_tdb) _input _p _train

feature_dropout ::
  -- | input
  Tensor ->
  -- | p
  Double ->
  -- | train
  Bool ->
  Tensor
feature_dropout _input _p _train = unsafePerformIO $ (cast3 ATen.feature_dropout_tdb) _input _p _train

alpha_dropout ::
  -- | input
  Tensor ->
  -- | p
  Double ->
  -- | train
  Bool ->
  Tensor
alpha_dropout _input _p _train = unsafePerformIO $ (cast3 ATen.alpha_dropout_tdb) _input _p _train

feature_alpha_dropout ::
  -- | input
  Tensor ->
  -- | p
  Double ->
  -- | train
  Bool ->
  Tensor
feature_alpha_dropout _input _p _train = unsafePerformIO $ (cast3 ATen.feature_alpha_dropout_tdb) _input _p _train

abs ::
  -- | self
  Tensor ->
  Tensor
abs _self = unsafePerformIO $ (cast1 ATen.abs_t) _self

absolute ::
  -- | self
  Tensor ->
  Tensor
absolute _self = unsafePerformIO $ (cast1 ATen.absolute_t) _self

angle ::
  -- | self
  Tensor ->
  Tensor
angle _self = unsafePerformIO $ (cast1 ATen.angle_t) _self

view_as_real ::
  -- | self
  Tensor ->
  Tensor
view_as_real _self = unsafePerformIO $ (cast1 ATen.view_as_real_t) _self

view_as_complex ::
  -- | self
  Tensor ->
  Tensor
view_as_complex _self = unsafePerformIO $ (cast1 ATen.view_as_complex_t) _self

sgn ::
  -- | self
  Tensor ->
  Tensor
sgn _self = unsafePerformIO $ (cast1 ATen.sgn_t) _self

real ::
  -- | self
  Tensor ->
  Tensor
real _self = unsafePerformIO $ (cast1 ATen.real_t) _self

imag ::
  -- | self
  Tensor ->
  Tensor
imag _self = unsafePerformIO $ (cast1 ATen.imag_t) _self

conj ::
  -- | self
  Tensor ->
  Tensor
conj _self = unsafePerformIO $ (cast1 ATen.conj_t) _self

conj_physical ::
  -- | self
  Tensor ->
  Tensor
conj_physical _self = unsafePerformIO $ (cast1 ATen.conj_physical_t) _self

resolve_conj ::
  -- | self
  Tensor ->
  Tensor
resolve_conj _self = unsafePerformIO $ (cast1 ATen.resolve_conj_t) _self

resolve_neg ::
  -- | self
  Tensor ->
  Tensor
resolve_neg _self = unsafePerformIO $ (cast1 ATen.resolve_neg_t) _self

acos ::
  -- | self
  Tensor ->
  Tensor
acos _self = unsafePerformIO $ (cast1 ATen.acos_t) _self

arccos ::
  -- | self
  Tensor ->
  Tensor
arccos _self = unsafePerformIO $ (cast1 ATen.arccos_t) _self

avg_pool1d ::
  -- | self
  Tensor ->
  -- | kernel_size
  Int ->
  -- | stride
  Int ->
  -- | padding
  Int ->
  -- | ceil_mode
  Bool ->
  -- | count_include_pad
  Bool ->
  Tensor
avg_pool1d _self _kernel_size _stride _padding _ceil_mode _count_include_pad = unsafePerformIO $ (cast6 ATen.avg_pool1d_tlllbb) _self _kernel_size _stride _padding _ceil_mode _count_include_pad

adaptive_avg_pool1d ::
  -- | self
  Tensor ->
  -- | output_size
  Int ->
  Tensor
adaptive_avg_pool1d _self _output_size = unsafePerformIO $ (cast2 ATen.adaptive_avg_pool1d_tl) _self _output_size

adaptive_max_pool1d ::
  -- | self
  Tensor ->
  -- | output_size
  Int ->
  (Tensor, Tensor)
adaptive_max_pool1d _self _output_size = unsafePerformIO $ (cast2 ATen.adaptive_max_pool1d_tl) _self _output_size

add ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | alpha
  Float ->
  Tensor
add _self _other _alpha = unsafePerformIO $ (cast3 ATen.add_tts) _self _other _alpha

addScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  -- | alpha
  Float ->
  Tensor
addScalar _self _other _alpha = unsafePerformIO $ (cast3 ATen.add_tss) _self _other _alpha

addmv ::
  -- | self
  Tensor ->
  -- | mat
  Tensor ->
  -- | vec
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
addmv _self _mat _vec _beta _alpha = unsafePerformIO $ (cast5 ATen.addmv_tttss) _self _mat _vec _beta _alpha

addr ::
  -- | self
  Tensor ->
  -- | vec1
  Tensor ->
  -- | vec2
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
addr _self _vec1 _vec2 _beta _alpha = unsafePerformIO $ (cast5 ATen.addr_tttss) _self _vec1 _vec2 _beta _alpha

affine_grid_generator ::
  -- | theta
  Tensor ->
  -- | size
  [Int] ->
  -- | align_corners
  Bool ->
  Tensor
affine_grid_generator _theta _size _align_corners = unsafePerformIO $ (cast3 ATen.affine_grid_generator_tlb) _theta _size _align_corners

allDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
allDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.all_tlb) _self _dim _keepdim

allWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  Tensor
allWithDimname _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.all_tnb) _self _dim _keepdim

allclose ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | rtol
  Double ->
  -- | atol
  Double ->
  -- | equal_nan
  Bool ->
  Bool
allclose _self _other _rtol _atol _equal_nan = unsafePerformIO $ (cast5 ATen.allclose_ttddb) _self _other _rtol _atol _equal_nan

anyDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
anyDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.any_tlb) _self _dim _keepdim

anyWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  Tensor
anyWithDimname _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.any_tnb) _self _dim _keepdim

argmax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
argmax _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.argmax_tlb) _self _dim _keepdim

argmin ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
argmin _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.argmin_tlb) _self _dim _keepdim

acosh ::
  -- | self
  Tensor ->
  Tensor
acosh _self = unsafePerformIO $ (cast1 ATen.acosh_t) _self

arccosh ::
  -- | self
  Tensor ->
  Tensor
arccosh _self = unsafePerformIO $ (cast1 ATen.arccosh_t) _self

asinh ::
  -- | self
  Tensor ->
  Tensor
asinh _self = unsafePerformIO $ (cast1 ATen.asinh_t) _self

arcsinh ::
  -- | self
  Tensor ->
  Tensor
arcsinh _self = unsafePerformIO $ (cast1 ATen.arcsinh_t) _self

atanh ::
  -- | self
  Tensor ->
  Tensor
atanh _self = unsafePerformIO $ (cast1 ATen.atanh_t) _self

arctanh ::
  -- | self
  Tensor ->
  Tensor
arctanh _self = unsafePerformIO $ (cast1 ATen.arctanh_t) _self

as_strided ::
  -- | self
  Tensor ->
  -- | size
  [Int] ->
  -- | stride
  [Int] ->
  -- | storage_offset
  Int ->
  Tensor
as_strided _self _size _stride _storage_offset = unsafePerformIO $ (cast4 ATen.as_strided_tlll) _self _size _stride _storage_offset

asin ::
  -- | self
  Tensor ->
  Tensor
asin _self = unsafePerformIO $ (cast1 ATen.asin_t) _self

arcsin ::
  -- | self
  Tensor ->
  Tensor
arcsin _self = unsafePerformIO $ (cast1 ATen.arcsin_t) _self

atan ::
  -- | self
  Tensor ->
  Tensor
atan _self = unsafePerformIO $ (cast1 ATen.atan_t) _self

arctan ::
  -- | self
  Tensor ->
  Tensor
arctan _self = unsafePerformIO $ (cast1 ATen.arctan_t) _self

atleast_1d_t ::
  -- | self
  Tensor ->
  Tensor
atleast_1d_t _self = unsafePerformIO $ (cast1 ATen.atleast_1d_t) _self

atleast_1d_l ::
  -- | tensors
  [Tensor] ->
  [Tensor]
atleast_1d_l _tensors = unsafePerformIO $ (cast1 ATen.atleast_1d_l) _tensors

atleast_2d_t ::
  -- | self
  Tensor ->
  Tensor
atleast_2d_t _self = unsafePerformIO $ (cast1 ATen.atleast_2d_t) _self

atleast_2d_l ::
  -- | tensors
  [Tensor] ->
  [Tensor]
atleast_2d_l _tensors = unsafePerformIO $ (cast1 ATen.atleast_2d_l) _tensors

atleast_3d_t ::
  -- | self
  Tensor ->
  Tensor
atleast_3d_t _self = unsafePerformIO $ (cast1 ATen.atleast_3d_t) _self

atleast_3d_l ::
  -- | tensors
  [Tensor] ->
  [Tensor]
atleast_3d_l _tensors = unsafePerformIO $ (cast1 ATen.atleast_3d_l) _tensors

baddbmm ::
  -- | self
  Tensor ->
  -- | batch1
  Tensor ->
  -- | batch2
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
baddbmm _self _batch1 _batch2 _beta _alpha = unsafePerformIO $ (cast5 ATen.baddbmm_tttss) _self _batch1 _batch2 _beta _alpha

batch_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | training
  Bool ->
  -- | momentum
  Double ->
  -- | eps
  Double ->
  -- | cudnn_enabled
  Bool ->
  Tensor
batch_norm _input _weight _bias _running_mean _running_var _training _momentum _eps _cudnn_enabled = unsafePerformIO $ (cast9 ATen.batch_norm_tttttbddb) _input _weight _bias _running_mean _running_var _training _momentum _eps _cudnn_enabled

quantized_batch_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | mean
  Tensor ->
  -- | var
  Tensor ->
  -- | eps
  Double ->
  -- | output_scale
  Double ->
  -- | output_zero_point
  Int ->
  Tensor
quantized_batch_norm _input _weight _bias _mean _var _eps _output_scale _output_zero_point = unsafePerformIO $ (cast8 ATen.quantized_batch_norm_tttttddl) _input _weight _bias _mean _var _eps _output_scale _output_zero_point

bilinear ::
  -- | input1
  Tensor ->
  -- | input2
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  Tensor
bilinear _input1 _input2 _weight _bias = unsafePerformIO $ (cast4 ATen.bilinear_tttt) _input1 _input2 _weight _bias

binary_cross_entropy ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  Tensor
binary_cross_entropy _self _target _weight _reduction = unsafePerformIO $ (cast4 ATen.binary_cross_entropy_tttl) _self _target _weight _reduction

binary_cross_entropy_with_logits ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | pos_weight
  Tensor ->
  -- | reduction
  Int ->
  Tensor
binary_cross_entropy_with_logits _self _target _weight _pos_weight _reduction = unsafePerformIO $ (cast5 ATen.binary_cross_entropy_with_logits_ttttl) _self _target _weight _pos_weight _reduction

bincount ::
  -- | self
  Tensor ->
  -- | weights
  Tensor ->
  -- | minlength
  Int ->
  Tensor
bincount _self _weights _minlength = unsafePerformIO $ (cast3 ATen.bincount_ttl) _self _weights _minlength

bitwise_not ::
  -- | self
  Tensor ->
  Tensor
bitwise_not _self = unsafePerformIO $ (cast1 ATen.bitwise_not_t) _self

copysign_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
copysign_tt _self _other = unsafePerformIO $ (cast2 ATen.copysign_tt) _self _other

copysign_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
copysign_ts _self _other = unsafePerformIO $ (cast2 ATen.copysign_ts) _self _other

logical_not ::
  -- | self
  Tensor ->
  Tensor
logical_not _self = unsafePerformIO $ (cast1 ATen.logical_not_t) _self

logical_xor ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
logical_xor _self _other = unsafePerformIO $ (cast2 ATen.logical_xor_tt) _self _other

logical_and ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
logical_and _self _other = unsafePerformIO $ (cast2 ATen.logical_and_tt) _self _other

logical_or ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
logical_or _self _other = unsafePerformIO $ (cast2 ATen.logical_or_tt) _self _other

bmm ::
  -- | self
  Tensor ->
  -- | mat2
  Tensor ->
  Tensor
bmm _self _mat2 = unsafePerformIO $ (cast2 ATen.bmm_tt) _self _mat2

broadcast_tensors ::
  -- | tensors
  [Tensor] ->
  [Tensor]
broadcast_tensors _tensors = unsafePerformIO $ (cast1 ATen.broadcast_tensors_l) _tensors

broadcast_to ::
  -- | self
  Tensor ->
  -- | size
  [Int] ->
  Tensor
broadcast_to _self _size = unsafePerformIO $ (cast2 ATen.broadcast_to_tl) _self _size

cat ::
  -- | tensors
  [Tensor] ->
  -- | dim
  Int ->
  Tensor
cat _tensors _dim = unsafePerformIO $ (cast2 ATen.cat_ll) _tensors _dim

catWithDimname ::
  -- | tensors
  [Tensor] ->
  -- | dim
  Dimname ->
  Tensor
catWithDimname _tensors _dim = unsafePerformIO $ (cast2 ATen.cat_ln) _tensors _dim

concat_ll ::
  -- | tensors
  [Tensor] ->
  -- | dim
  Int ->
  Tensor
concat_ll _tensors _dim = unsafePerformIO $ (cast2 ATen.concat_ll) _tensors _dim

concat_ln ::
  -- | tensors
  [Tensor] ->
  -- | dim
  Dimname ->
  Tensor
concat_ln _tensors _dim = unsafePerformIO $ (cast2 ATen.concat_ln) _tensors _dim

block_diag ::
  -- | tensors
  [Tensor] ->
  Tensor
block_diag _tensors = unsafePerformIO $ (cast1 ATen.block_diag_l) _tensors

ceil ::
  -- | self
  Tensor ->
  Tensor
ceil _self = unsafePerformIO $ (cast1 ATen.ceil_t) _self

chain_matmul ::
  -- | matrices
  [Tensor] ->
  Tensor
chain_matmul _matrices = unsafePerformIO $ (cast1 ATen.chain_matmul_l) _matrices

unsafe_chunk ::
  -- | self
  Tensor ->
  -- | chunks
  Int ->
  -- | dim
  Int ->
  [Tensor]
unsafe_chunk _self _chunks _dim = unsafePerformIO $ (cast3 ATen.unsafe_chunk_tll) _self _chunks _dim

chunk ::
  -- | self
  Tensor ->
  -- | chunks
  Int ->
  -- | dim
  Int ->
  [Tensor]
chunk _self _chunks _dim = unsafePerformIO $ (cast3 ATen.chunk_tll) _self _chunks _dim

tensor_split_tll ::
  -- | self
  Tensor ->
  -- | sections
  Int ->
  -- | dim
  Int ->
  [Tensor]
tensor_split_tll _self _sections _dim = unsafePerformIO $ (cast3 ATen.tensor_split_tll) _self _sections _dim

-- tensor_split_tll
--   :: Tensor -- ^ self
--   -> [Int] -- ^ indices
--   -> Int -- ^ dim
--   -> [Tensor]
-- tensor_split_tll _self _indices _dim = unsafePerformIO $ (cast3 ATen.tensor_split_tll) _self _indices _dim

-- tensor_split_ttl
--   :: Tensor -- ^ self
--   -> Tensor -- ^ tensor_indices_or_sections
--   -> Int -- ^ dim
--   -> [Tensor]
-- tensor_split_ttl _self _tensor_indices_or_sections _dim = unsafePerformIO $ (cast3 ATen.tensor_split_ttl) _self _tensor_indices_or_sections _dim

clamp_tss ::
  -- | self
  Tensor ->
  -- | min
  Float ->
  -- | max
  Float ->
  Tensor
clamp_tss _self _min _max = unsafePerformIO $ (cast3 ATen.clamp_tss) _self _min _max

clamp_ttt ::
  -- | self
  Tensor ->
  -- | min
  Tensor ->
  -- | max
  Tensor ->
  Tensor
clamp_ttt _self _min _max = unsafePerformIO $ (cast3 ATen.clamp_ttt) _self _min _max

clamp_max_ts ::
  -- | self
  Tensor ->
  -- | max
  Float ->
  Tensor
clamp_max_ts _self _max = unsafePerformIO $ (cast2 ATen.clamp_max_ts) _self _max

clamp_max_tt ::
  -- | self
  Tensor ->
  -- | max
  Tensor ->
  Tensor
clamp_max_tt _self _max = unsafePerformIO $ (cast2 ATen.clamp_max_tt) _self _max

clamp_min_ts ::
  -- | self
  Tensor ->
  -- | min
  Float ->
  Tensor
clamp_min_ts _self _min = unsafePerformIO $ (cast2 ATen.clamp_min_ts) _self _min

clamp_min_tt ::
  -- | self
  Tensor ->
  -- | min
  Tensor ->
  Tensor
clamp_min_tt _self _min = unsafePerformIO $ (cast2 ATen.clamp_min_tt) _self _min

clip_tss ::
  -- | self
  Tensor ->
  -- | min
  Float ->
  -- | max
  Float ->
  Tensor
clip_tss _self _min _max = unsafePerformIO $ (cast3 ATen.clip_tss) _self _min _max

clip_ttt ::
  -- | self
  Tensor ->
  -- | min
  Tensor ->
  -- | max
  Tensor ->
  Tensor
clip_ttt _self _min _max = unsafePerformIO $ (cast3 ATen.clip_ttt) _self _min _max

cudnn_is_acceptable ::
  -- | self
  Tensor ->
  Bool
cudnn_is_acceptable _self = unsafePerformIO $ (cast1 ATen.cudnn_is_acceptable_t) _self

complex ::
  -- | real
  Tensor ->
  -- | imag
  Tensor ->
  Tensor
complex _real _imag = unsafePerformIO $ (cast2 ATen.complex_tt) _real _imag

polar ::
  -- | abs
  Tensor ->
  -- | angle
  Tensor ->
  Tensor
polar _abs _angle = unsafePerformIO $ (cast2 ATen.polar_tt) _abs _angle

constant_pad_nd ::
  -- | self
  Tensor ->
  -- | pad
  [Int] ->
  -- | value
  Float ->
  Tensor
constant_pad_nd _self _pad _value = unsafePerformIO $ (cast3 ATen.constant_pad_nd_tls) _self _pad _value

convolution ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  [Int] ->
  -- | padding
  [Int] ->
  -- | dilation
  [Int] ->
  -- | transposed
  Bool ->
  -- | output_padding
  [Int] ->
  -- | groups
  Int ->
  Tensor
convolution _input _weight _bias _stride _padding _dilation _transposed _output_padding _groups = unsafePerformIO $ (cast9 ATen.convolution_tttlllbll) _input _weight _bias _stride _padding _dilation _transposed _output_padding _groups

convolution_overrideable ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  [Int] ->
  -- | padding
  [Int] ->
  -- | dilation
  [Int] ->
  -- | transposed
  Bool ->
  -- | output_padding
  [Int] ->
  -- | groups
  Int ->
  Tensor
convolution_overrideable _input _weight _bias _stride _padding _dilation _transposed _output_padding _groups = unsafePerformIO $ (cast9 ATen.convolution_overrideable_tttlllbll) _input _weight _bias _stride _padding _dilation _transposed _output_padding _groups

convolution_backward_overrideable ::
  -- | grad_output
  Tensor ->
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | stride
  [Int] ->
  -- | padding
  [Int] ->
  -- | dilation
  [Int] ->
  -- | transposed
  Bool ->
  -- | output_padding
  [Int] ->
  -- | groups
  Int ->
  -- | output_mask
  (Bool, Bool, Bool) ->
  (Tensor, Tensor, Tensor)
convolution_backward_overrideable _grad_output _input _weight _stride _padding _dilation _transposed _output_padding _groups _output_mask = unsafePerformIO $ (cast10 ATen.convolution_backward_overrideable_tttlllblla) _grad_output _input _weight _stride _padding _dilation _transposed _output_padding _groups _output_mask

conv1d_tttllll ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  Int ->
  -- | padding
  Int ->
  -- | dilation
  Int ->
  -- | groups
  Int ->
  Tensor
conv1d_tttllll _input _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.conv1d_tttllll) _input _weight _bias _stride _padding _dilation _groups

conv2d ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | groups
  Int ->
  Tensor
conv2d _input _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.conv2d_tttllll) _input _weight _bias _stride _padding _dilation _groups

conv3d_tttllll ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  -- | groups
  Int ->
  Tensor
conv3d_tttllll _input _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.conv3d_tttllll) _input _weight _bias _stride _padding _dilation _groups

conv1d_tttlsll ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  Int ->
  -- | padding
  String ->
  -- | dilation
  Int ->
  -- | groups
  Int ->
  Tensor
conv1d_tttlsll _input _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.conv1d_tttlsll) _input _weight _bias _stride _padding _dilation _groups

conv2d_tttlsll ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int) ->
  -- | padding
  String ->
  -- | dilation
  (Int, Int) ->
  -- | groups
  Int ->
  Tensor
conv2d_tttlsll _input _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.conv2d_tttlsll) _input _weight _bias _stride _padding _dilation _groups

conv3d_tttlsll ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  String ->
  -- | dilation
  (Int, Int, Int) ->
  -- | groups
  Int ->
  Tensor
conv3d_tttlsll _input _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.conv3d_tttlsll) _input _weight _bias _stride _padding _dilation _groups

conv_tbc ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | pad
  Int ->
  Tensor
conv_tbc _self _weight _bias _pad = unsafePerformIO $ (cast4 ATen.conv_tbc_tttl) _self _weight _bias _pad

conv_transpose1d ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  Int ->
  -- | padding
  Int ->
  -- | output_padding
  Int ->
  -- | groups
  Int ->
  -- | dilation
  Int ->
  Tensor
conv_transpose1d _input _weight _bias _stride _padding _output_padding _groups _dilation = unsafePerformIO $ (cast8 ATen.conv_transpose1d_tttlllll) _input _weight _bias _stride _padding _output_padding _groups _dilation

conv_transpose2d ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | output_padding
  (Int, Int) ->
  -- | groups
  Int ->
  -- | dilation
  (Int, Int) ->
  Tensor
conv_transpose2d _input _weight _bias _stride _padding _output_padding _groups _dilation = unsafePerformIO $ (cast8 ATen.conv_transpose2d_tttlllll) _input _weight _bias _stride _padding _output_padding _groups _dilation

conv_transpose3d ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | output_padding
  (Int, Int, Int) ->
  -- | groups
  Int ->
  -- | dilation
  (Int, Int, Int) ->
  Tensor
conv_transpose3d _input _weight _bias _stride _padding _output_padding _groups _dilation = unsafePerformIO $ (cast8 ATen.conv_transpose3d_tttlllll) _input _weight _bias _stride _padding _output_padding _groups _dilation

cos ::
  -- | self
  Tensor ->
  Tensor
cos _self = unsafePerformIO $ (cast1 ATen.cos_t) _self

cosh ::
  -- | self
  Tensor ->
  Tensor
cosh _self = unsafePerformIO $ (cast1 ATen.cosh_t) _self

cosine_embedding_loss ::
  -- | input1
  Tensor ->
  -- | input2
  Tensor ->
  -- | target
  Tensor ->
  -- | margin
  Double ->
  -- | reduction
  Int ->
  Tensor
cosine_embedding_loss _input1 _input2 _target _margin _reduction = unsafePerformIO $ (cast5 ATen.cosine_embedding_loss_tttdl) _input1 _input2 _target _margin _reduction

count_nonzero_tl ::
  -- | self
  Tensor ->
  -- | dim
  [Int] ->
  Tensor
count_nonzero_tl _self _dim = unsafePerformIO $ (cast2 ATen.count_nonzero_tl) _self _dim

-- count_nonzero_tl
--   :: Tensor -- ^ self
--   -> Int -- ^ dim
--   -> Tensor
-- count_nonzero_tl _self _dim = unsafePerformIO $ (cast2 ATen.count_nonzero_tl) _self _dim

cov ::
  -- | self
  Tensor ->
  -- | correction
  Int ->
  -- | fweights
  Tensor ->
  -- | aweights
  Tensor ->
  Tensor
cov _self _correction _fweights _aweights = unsafePerformIO $ (cast4 ATen.cov_tltt) _self _correction _fweights _aweights

corrcoef ::
  -- | self
  Tensor ->
  Tensor
corrcoef _self = unsafePerformIO $ (cast1 ATen.corrcoef_t) _self

cudnn_affine_grid_generator ::
  -- | theta
  Tensor ->
  -- | N
  Int ->
  -- | C
  Int ->
  -- | H
  Int ->
  -- | W
  Int ->
  Tensor
cudnn_affine_grid_generator _theta _N _C _H _W = unsafePerformIO $ (cast5 ATen.cudnn_affine_grid_generator_tllll) _theta _N _C _H _W

cudnn_batch_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | training
  Bool ->
  -- | exponential_average_factor
  Double ->
  -- | epsilon
  Double ->
  (Tensor, Tensor, Tensor, Tensor)
cudnn_batch_norm _input _weight _bias _running_mean _running_var _training _exponential_average_factor _epsilon = unsafePerformIO $ (cast8 ATen.cudnn_batch_norm_tttttbdd) _input _weight _bias _running_mean _running_var _training _exponential_average_factor _epsilon

cudnn_convolution ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | padding
  [Int] ->
  -- | stride
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  -- | benchmark
  Bool ->
  -- | deterministic
  Bool ->
  -- | allow_tf32
  Bool ->
  Tensor
cudnn_convolution _self _weight _padding _stride _dilation _groups _benchmark _deterministic _allow_tf32 = unsafePerformIO $ (cast9 ATen.cudnn_convolution_ttllllbbb) _self _weight _padding _stride _dilation _groups _benchmark _deterministic _allow_tf32

cudnn_convolution_transpose ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | padding
  [Int] ->
  -- | output_padding
  [Int] ->
  -- | stride
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  -- | benchmark
  Bool ->
  -- | deterministic
  Bool ->
  -- | allow_tf32
  Bool ->
  Tensor
cudnn_convolution_transpose _self _weight _padding _output_padding _stride _dilation _groups _benchmark _deterministic _allow_tf32 = unsafePerformIO $ (cast10 ATen.cudnn_convolution_transpose_ttlllllbbb) _self _weight _padding _output_padding _stride _dilation _groups _benchmark _deterministic _allow_tf32

cudnn_convolution_relu ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | stride
  [Int] ->
  -- | padding
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  Tensor
cudnn_convolution_relu _self _weight _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast7 ATen.cudnn_convolution_relu_tttllll) _self _weight _bias _stride _padding _dilation _groups

cudnn_convolution_add_relu ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | z
  Tensor ->
  -- | alpha
  Float ->
  -- | bias
  Tensor ->
  -- | stride
  [Int] ->
  -- | padding
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  Tensor
cudnn_convolution_add_relu _self _weight _z _alpha _bias _stride _padding _dilation _groups = unsafePerformIO $ (cast9 ATen.cudnn_convolution_add_relu_tttstllll) _self _weight _z _alpha _bias _stride _padding _dilation _groups

cudnn_grid_sampler ::
  -- | self
  Tensor ->
  -- | grid
  Tensor ->
  Tensor
cudnn_grid_sampler _self _grid = unsafePerformIO $ (cast2 ATen.cudnn_grid_sampler_tt) _self _grid

cummax_tl ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  (Tensor, Tensor)
cummax_tl _self _dim = unsafePerformIO $ (cast2 ATen.cummax_tl) _self _dim

cummax_tn ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  (Tensor, Tensor)
cummax_tn _self _dim = unsafePerformIO $ (cast2 ATen.cummax_tn) _self _dim

cummin_tl ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  (Tensor, Tensor)
cummin_tl _self _dim = unsafePerformIO $ (cast2 ATen.cummin_tl) _self _dim

cummin_tn ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  (Tensor, Tensor)
cummin_tn _self _dim = unsafePerformIO $ (cast2 ATen.cummin_tn) _self _dim

cumprod ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | dtype
  DType ->
  Tensor
cumprod _self _dim _dtype = unsafePerformIO $ (cast3 ATen.cumprod_tls) _self _dim _dtype

cumprodWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | dtype
  DType ->
  Tensor
cumprodWithDimname _self _dim _dtype = unsafePerformIO $ (cast3 ATen.cumprod_tns) _self _dim _dtype

cumsum ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | dtype
  DType ->
  Tensor
cumsum _self _dim _dtype = unsafePerformIO $ (cast3 ATen.cumsum_tls) _self _dim _dtype

cumsumWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | dtype
  DType ->
  Tensor
cumsumWithDimname _self _dim _dtype = unsafePerformIO $ (cast3 ATen.cumsum_tns) _self _dim _dtype

cumulative_trapezoid_ttl ::
  -- | y
  Tensor ->
  -- | x
  Tensor ->
  -- | dim
  Int ->
  Tensor
cumulative_trapezoid_ttl _y _x _dim = unsafePerformIO $ (cast3 ATen.cumulative_trapezoid_ttl) _y _x _dim

cumulative_trapezoid_tsl ::
  -- | y
  Tensor ->
  -- | dx
  Float ->
  -- | dim
  Int ->
  Tensor
cumulative_trapezoid_tsl _y _dx _dim = unsafePerformIO $ (cast3 ATen.cumulative_trapezoid_tsl) _y _dx _dim

ctcLoss' ::
  -- | log_probs
  Tensor ->
  -- | targets
  Tensor ->
  -- | input_lengths
  [Int] ->
  -- | target_lengths
  [Int] ->
  -- | blank
  Int ->
  -- | reduction
  Int ->
  -- | zero_infinity
  Bool ->
  Tensor
ctcLoss' _log_probs _targets _input_lengths _target_lengths _blank _reduction _zero_infinity = unsafePerformIO $ (cast7 ATen.ctc_loss_ttllllb) _log_probs _targets _input_lengths _target_lengths _blank _reduction _zero_infinity

ctcLoss ::
  -- | log_probs
  Tensor ->
  -- | targets
  Tensor ->
  -- | input_lengths
  Tensor ->
  -- | target_lengths
  Tensor ->
  -- | blank
  Int ->
  -- | reduction
  Int ->
  -- | zero_infinity
  Bool ->
  Tensor
ctcLoss _log_probs _targets _input_lengths _target_lengths _blank _reduction _zero_infinity = unsafePerformIO $ (cast7 ATen.ctc_loss_ttttllb) _log_probs _targets _input_lengths _target_lengths _blank _reduction _zero_infinity

diag_embed ::
  -- | self
  Tensor ->
  -- | offset
  Int ->
  -- | dim1
  Int ->
  -- | dim2
  Int ->
  Tensor
diag_embed _self _offset _dim1 _dim2 = unsafePerformIO $ (cast4 ATen.diag_embed_tlll) _self _offset _dim1 _dim2

diagflat ::
  -- | self
  Tensor ->
  -- | offset
  Int ->
  Tensor
diagflat _self _offset = unsafePerformIO $ (cast2 ATen.diagflat_tl) _self _offset

diagonal_tlll ::
  -- | self
  Tensor ->
  -- | offset
  Int ->
  -- | dim1
  Int ->
  -- | dim2
  Int ->
  Tensor
diagonal_tlll _self _offset _dim1 _dim2 = unsafePerformIO $ (cast4 ATen.diagonal_tlll) _self _offset _dim1 _dim2

linalg_diagonal ::
  -- | A
  Tensor ->
  -- | offset
  Int ->
  -- | dim1
  Int ->
  -- | dim2
  Int ->
  Tensor
linalg_diagonal _A _offset _dim1 _dim2 = unsafePerformIO $ (cast4 ATen.linalg_diagonal_tlll) _A _offset _dim1 _dim2

diagonal_tnnnl ::
  -- | self
  Tensor ->
  -- | outdim
  Dimname ->
  -- | dim1
  Dimname ->
  -- | dim2
  Dimname ->
  -- | offset
  Int ->
  Tensor
diagonal_tnnnl _self _outdim _dim1 _dim2 _offset = unsafePerformIO $ (cast5 ATen.diagonal_tnnnl) _self _outdim _dim1 _dim2 _offset

diff ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | prepend
  Tensor ->
  -- | append
  Tensor ->
  Tensor
diff _self _n _dim _prepend _append = unsafePerformIO $ (cast5 ATen.diff_tlltt) _self _n _dim _prepend _append

gradient_tsll ::
  -- | self
  Tensor ->
  -- | spacing
  Float ->
  -- | dim
  Int ->
  -- | edge_order
  Int ->
  [Tensor]
gradient_tsll _self _spacing _dim _edge_order = unsafePerformIO $ (cast4 ATen.gradient_tsll) _self _spacing _dim _edge_order

-- gradient_tsll
--   :: Tensor -- ^ self
--   -> Float -- ^ spacing
--   -> [Int] -- ^ dim
--   -> Int -- ^ edge_order
--   -> [Tensor]
-- gradient_tsll _self _spacing _dim _edge_order = unsafePerformIO $ (cast4 ATen.gradient_tsll) _self _spacing _dim _edge_order

gradient_tll ::
  -- | self
  Tensor ->
  -- | dim
  [Int] ->
  -- | edge_order
  Int ->
  [Tensor]
gradient_tll _self _dim _edge_order = unsafePerformIO $ (cast3 ATen.gradient_tll) _self _dim _edge_order

-- gradient_tAll
--   :: Tensor -- ^ self
--   -> [Scalar] -- ^ spacing
--   -> Int -- ^ dim
--   -> Int -- ^ edge_order
--   -> [Tensor]
-- gradient_tAll _self _spacing _dim _edge_order = unsafePerformIO $ (cast4 ATen.gradient_tAll) _self _spacing _dim _edge_order

-- gradient_tAll
--   :: Tensor -- ^ self
--   -> [Scalar] -- ^ spacing
--   -> [Int] -- ^ dim
--   -> Int -- ^ edge_order
--   -> [Tensor]
-- gradient_tAll _self _spacing _dim _edge_order = unsafePerformIO $ (cast4 ATen.gradient_tAll) _self _spacing _dim _edge_order

-- gradient_tlll
--   :: Tensor -- ^ self
--   -> [Tensor] -- ^ spacing
--   -> Int -- ^ dim
--   -> Int -- ^ edge_order
--   -> [Tensor]
-- gradient_tlll _self _spacing _dim _edge_order = unsafePerformIO $ (cast4 ATen.gradient_tlll) _self _spacing _dim _edge_order

-- gradient_tlll
--   :: Tensor -- ^ self
--   -> [Tensor] -- ^ spacing
--   -> [Int] -- ^ dim
--   -> Int -- ^ edge_order
--   -> [Tensor]
-- gradient_tlll _self _spacing _dim _edge_order = unsafePerformIO $ (cast4 ATen.gradient_tlll) _self _spacing _dim _edge_order

div ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
div _self _other = unsafePerformIO $ (cast2 ATen.div_tt) _self _other

div_tts ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | rounding_mode
  String ->
  Tensor
div_tts _self _other _rounding_mode = unsafePerformIO $ (cast3 ATen.div_tts) _self _other _rounding_mode

divScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
divScalar _self _other = unsafePerformIO $ (cast2 ATen.div_ts) _self _other

div_tss ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  -- | rounding_mode
  String ->
  Tensor
div_tss _self _other _rounding_mode = unsafePerformIO $ (cast3 ATen.div_tss) _self _other _rounding_mode

divide_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
divide_tt _self _other = unsafePerformIO $ (cast2 ATen.divide_tt) _self _other

divide_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
divide_ts _self _other = unsafePerformIO $ (cast2 ATen.divide_ts) _self _other

divide_tts ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | rounding_mode
  String ->
  Tensor
divide_tts _self _other _rounding_mode = unsafePerformIO $ (cast3 ATen.divide_tts) _self _other _rounding_mode

divide_tss ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  -- | rounding_mode
  String ->
  Tensor
divide_tss _self _other _rounding_mode = unsafePerformIO $ (cast3 ATen.divide_tss) _self _other _rounding_mode

true_divide_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
true_divide_tt _self _other = unsafePerformIO $ (cast2 ATen.true_divide_tt) _self _other

true_divide_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
true_divide_ts _self _other = unsafePerformIO $ (cast2 ATen.true_divide_ts) _self _other

dot ::
  -- | self
  Tensor ->
  -- | tensor
  Tensor ->
  Tensor
dot _self _tensor = unsafePerformIO $ (cast2 ATen.dot_tt) _self _tensor

vdot ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
vdot _self _other = unsafePerformIO $ (cast2 ATen.vdot_tt) _self _other

einsum ::
  -- | equation
  String ->
  -- | tensors
  [Tensor] ->
  Tensor
einsum _equation _tensors = unsafePerformIO $ (cast2 ATen.einsum_sl) _equation _tensors

embedding ::
  -- | weight
  Tensor ->
  -- | indices
  Tensor ->
  -- | padding_idx
  Int ->
  -- | scale_grad_by_freq
  Bool ->
  -- | sparse
  Bool ->
  Tensor
embedding _weight _indices _padding_idx _scale_grad_by_freq _sparse = unsafePerformIO $ (cast5 ATen.embedding_ttlbb) _weight _indices _padding_idx _scale_grad_by_freq _sparse

row_stack ::
  -- | tensors
  [Tensor] ->
  Tensor
row_stack _tensors = unsafePerformIO $ (cast1 ATen.row_stack_l) _tensors

embedding_bag_tttblbtb ::
  -- | weight
  Tensor ->
  -- | indices
  Tensor ->
  -- | offsets
  Tensor ->
  -- | scale_grad_by_freq
  Bool ->
  -- | mode
  Int ->
  -- | sparse
  Bool ->
  -- | per_sample_weights
  Tensor ->
  -- | include_last_offset
  Bool ->
  (Tensor, Tensor, Tensor, Tensor)
embedding_bag_tttblbtb _weight _indices _offsets _scale_grad_by_freq _mode _sparse _per_sample_weights _include_last_offset = unsafePerformIO $ (cast8 ATen.embedding_bag_tttblbtb) _weight _indices _offsets _scale_grad_by_freq _mode _sparse _per_sample_weights _include_last_offset

embedding_bag_tttblbtbl ::
  -- | weight
  Tensor ->
  -- | indices
  Tensor ->
  -- | offsets
  Tensor ->
  -- | scale_grad_by_freq
  Bool ->
  -- | mode
  Int ->
  -- | sparse
  Bool ->
  -- | per_sample_weights
  Tensor ->
  -- | include_last_offset
  Bool ->
  -- | padding_idx
  Int ->
  (Tensor, Tensor, Tensor, Tensor)
embedding_bag_tttblbtbl _weight _indices _offsets _scale_grad_by_freq _mode _sparse _per_sample_weights _include_last_offset _padding_idx = unsafePerformIO $ (cast9 ATen.embedding_bag_tttblbtbl) _weight _indices _offsets _scale_grad_by_freq _mode _sparse _per_sample_weights _include_last_offset _padding_idx

erf ::
  -- | self
  Tensor ->
  Tensor
erf _self = unsafePerformIO $ (cast1 ATen.erf_t) _self

erfc ::
  -- | self
  Tensor ->
  Tensor
erfc _self = unsafePerformIO $ (cast1 ATen.erfc_t) _self

exp ::
  -- | self
  Tensor ->
  Tensor
exp _self = unsafePerformIO $ (cast1 ATen.exp_t) _self

exp2 ::
  -- | self
  Tensor ->
  Tensor
exp2 _self = unsafePerformIO $ (cast1 ATen.exp2_t) _self

expm1 ::
  -- | self
  Tensor ->
  Tensor
expm1 _self = unsafePerformIO $ (cast1 ATen.expm1_t) _self

flatten ::
  -- | self
  Tensor ->
  -- | start_dim
  Int ->
  -- | end_dim
  Int ->
  Tensor
flatten _self _start_dim _end_dim = unsafePerformIO $ (cast3 ATen.flatten_tll) _self _start_dim _end_dim

flattenTo ::
  -- | self
  Tensor ->
  -- | start_dim
  Int ->
  -- | end_dim
  Int ->
  -- | out_dim
  Dimname ->
  Tensor
flattenTo _self _start_dim _end_dim _out_dim = unsafePerformIO $ (cast4 ATen.flatten_tlln) _self _start_dim _end_dim _out_dim

flattenToWithDimname ::
  -- | self
  Tensor ->
  -- | start_dim
  Dimname ->
  -- | end_dim
  Dimname ->
  -- | out_dim
  Dimname ->
  Tensor
flattenToWithDimname _self _start_dim _end_dim _out_dim = unsafePerformIO $ (cast4 ATen.flatten_tnnn) _self _start_dim _end_dim _out_dim

flattenToWithDimnames ::
  -- | self
  Tensor ->
  -- | dims
  [Dimname] ->
  -- | out_dim
  Dimname ->
  Tensor
flattenToWithDimnames _self _dims _out_dim = unsafePerformIO $ (cast3 ATen.flatten_tNn) _self _dims _out_dim

floor ::
  -- | self
  Tensor ->
  Tensor
floor _self = unsafePerformIO $ (cast1 ATen.floor_t) _self

floor_divide_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
floor_divide_tt _self _other = unsafePerformIO $ (cast2 ATen.floor_divide_tt) _self _other

floor_divide_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
floor_divide_ts _self _other = unsafePerformIO $ (cast2 ATen.floor_divide_ts) _self _other

frac ::
  -- | self
  Tensor ->
  Tensor
frac _self = unsafePerformIO $ (cast1 ATen.frac_t) _self

gcd ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
gcd _self _other = unsafePerformIO $ (cast2 ATen.gcd_tt) _self _other

lcm ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
lcm _self _other = unsafePerformIO $ (cast2 ATen.lcm_tt) _self _other

grid_sampler ::
  -- | input
  Tensor ->
  -- | grid
  Tensor ->
  -- | interpolation_mode
  Int ->
  -- | padding_mode
  Int ->
  -- | align_corners
  Bool ->
  Tensor
grid_sampler _input _grid _interpolation_mode _padding_mode _align_corners = unsafePerformIO $ (cast5 ATen.grid_sampler_ttllb) _input _grid _interpolation_mode _padding_mode _align_corners

grid_sampler_2d ::
  -- | input
  Tensor ->
  -- | grid
  Tensor ->
  -- | interpolation_mode
  Int ->
  -- | padding_mode
  Int ->
  -- | align_corners
  Bool ->
  Tensor
grid_sampler_2d _input _grid _interpolation_mode _padding_mode _align_corners = unsafePerformIO $ (cast5 ATen.grid_sampler_2d_ttllb) _input _grid _interpolation_mode _padding_mode _align_corners

grid_sampler_3d ::
  -- | input
  Tensor ->
  -- | grid
  Tensor ->
  -- | interpolation_mode
  Int ->
  -- | padding_mode
  Int ->
  -- | align_corners
  Bool ->
  Tensor
grid_sampler_3d _input _grid _interpolation_mode _padding_mode _align_corners = unsafePerformIO $ (cast5 ATen.grid_sampler_3d_ttllb) _input _grid _interpolation_mode _padding_mode _align_corners

hinge_embedding_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | margin
  Double ->
  -- | reduction
  Int ->
  Tensor
hinge_embedding_loss _self _target _margin _reduction = unsafePerformIO $ (cast4 ATen.hinge_embedding_loss_ttdl) _self _target _margin _reduction

group_norm ::
  -- | input
  Tensor ->
  -- | num_groups
  Int ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | eps
  Double ->
  -- | cudnn_enabled
  Bool ->
  Tensor
group_norm _input _num_groups _weight _bias _eps _cudnn_enabled = unsafePerformIO $ (cast6 ATen.group_norm_tlttdb) _input _num_groups _weight _bias _eps _cudnn_enabled

native_group_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | N
  Int ->
  -- | C
  Int ->
  -- | HxW
  Int ->
  -- | group
  Int ->
  -- | eps
  Double ->
  (Tensor, Tensor, Tensor)
native_group_norm _input _weight _bias _N _C _HxW _group _eps = unsafePerformIO $ (cast8 ATen.native_group_norm_tttlllld) _input _weight _bias _N _C _HxW _group _eps

index ::
  -- | self
  Tensor ->
  -- | indices
  [Tensor] ->
  Tensor
index _self _indices = unsafePerformIO $ (cast2 ATen.index_tl) _self _indices

indexCopy ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | source
  Tensor ->
  Tensor
indexCopy _self _dim _index _source = unsafePerformIO $ (cast4 ATen.index_copy_tltt) _self _dim _index _source

indexCopyWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | source
  Tensor ->
  Tensor
indexCopyWithDimname _self _dim _index _source = unsafePerformIO $ (cast4 ATen.index_copy_tntt) _self _dim _index _source

index_put ::
  -- | self
  Tensor ->
  -- | indices
  [Tensor] ->
  -- | values
  Tensor ->
  -- | accumulate
  Bool ->
  Tensor
index_put _self _indices _values _accumulate = unsafePerformIO $ (cast4 ATen.index_put_tltb) _self _indices _values _accumulate

instance_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | use_input_stats
  Bool ->
  -- | momentum
  Double ->
  -- | eps
  Double ->
  -- | cudnn_enabled
  Bool ->
  Tensor
instance_norm _input _weight _bias _running_mean _running_var _use_input_stats _momentum _eps _cudnn_enabled = unsafePerformIO $ (cast9 ATen.instance_norm_tttttbddb) _input _weight _bias _running_mean _running_var _use_input_stats _momentum _eps _cudnn_enabled

inverse ::
  -- | self
  Tensor ->
  Tensor
inverse _self = unsafePerformIO $ (cast1 ATen.inverse_t) _self

isclose ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | rtol
  Double ->
  -- | atol
  Double ->
  -- | equal_nan
  Bool ->
  Tensor
isclose _self _other _rtol _atol _equal_nan = unsafePerformIO $ (cast5 ATen.isclose_ttddb) _self _other _rtol _atol _equal_nan

isin_ttbb ::
  -- | elements
  Tensor ->
  -- | test_elements
  Tensor ->
  -- | assume_unique
  Bool ->
  -- | invert
  Bool ->
  Tensor
isin_ttbb _elements _test_elements _assume_unique _invert = unsafePerformIO $ (cast4 ATen.isin_ttbb) _elements _test_elements _assume_unique _invert

isin_tsbb ::
  -- | elements
  Tensor ->
  -- | test_element
  Float ->
  -- | assume_unique
  Bool ->
  -- | invert
  Bool ->
  Tensor
isin_tsbb _elements _test_element _assume_unique _invert = unsafePerformIO $ (cast4 ATen.isin_tsbb) _elements _test_element _assume_unique _invert

isin_stbb ::
  -- | element
  Float ->
  -- | test_elements
  Tensor ->
  -- | assume_unique
  Bool ->
  -- | invert
  Bool ->
  Tensor
isin_stbb _element _test_elements _assume_unique _invert = unsafePerformIO $ (cast4 ATen.isin_stbb) _element _test_elements _assume_unique _invert

isnan ::
  -- | self
  Tensor ->
  Tensor
isnan _self = unsafePerformIO $ (cast1 ATen.isnan_t) _self

is_distributed ::
  -- | self
  Tensor ->
  Bool
is_distributed _self = unsafePerformIO $ (cast1 ATen.is_distributed_t) _self

is_floating_point ::
  -- | self
  Tensor ->
  Bool
is_floating_point _self = unsafePerformIO $ (cast1 ATen.is_floating_point_t) _self

is_complex ::
  -- | self
  Tensor ->
  Bool
is_complex _self = unsafePerformIO $ (cast1 ATen.is_complex_t) _self

is_conj ::
  -- | self
  Tensor ->
  Bool
is_conj _self = unsafePerformIO $ (cast1 ATen.is_conj_t) _self

is_neg ::
  -- | self
  Tensor ->
  Bool
is_neg _self = unsafePerformIO $ (cast1 ATen.is_neg_t) _self

isreal ::
  -- | self
  Tensor ->
  Tensor
isreal _self = unsafePerformIO $ (cast1 ATen.isreal_t) _self

is_nonzero ::
  -- | self
  Tensor ->
  Bool
is_nonzero _self = unsafePerformIO $ (cast1 ATen.is_nonzero_t) _self

is_same_size ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Bool
is_same_size _self _other = unsafePerformIO $ (cast2 ATen.is_same_size_tt) _self _other

is_signed ::
  -- | self
  Tensor ->
  Bool
is_signed _self = unsafePerformIO $ (cast1 ATen.is_signed_t) _self

is_inference ::
  -- | self
  Tensor ->
  Bool
is_inference _self = unsafePerformIO $ (cast1 ATen.is_inference_t) _self

kl_div ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  -- | log_target
  Bool ->
  Tensor
kl_div _self _target _reduction _log_target = unsafePerformIO $ (cast4 ATen.kl_div_ttlb) _self _target _reduction _log_target

kron ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
kron _self _other = unsafePerformIO $ (cast2 ATen.kron_tt) _self _other

kthvalue ::
  -- | self
  Tensor ->
  -- | k
  Int ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
kthvalue _self _k _dim _keepdim = unsafePerformIO $ (cast4 ATen.kthvalue_tllb) _self _k _dim _keepdim

kthvalueWithDimname ::
  -- | self
  Tensor ->
  -- | k
  Int ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
kthvalueWithDimname _self _k _dim _keepdim = unsafePerformIO $ (cast4 ATen.kthvalue_tlnb) _self _k _dim _keepdim

layer_norm ::
  -- | input
  Tensor ->
  -- | normalized_shape
  [Int] ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | eps
  Double ->
  -- | cudnn_enable
  Bool ->
  Tensor
layer_norm _input _normalized_shape _weight _bias _eps _cudnn_enable = unsafePerformIO $ (cast6 ATen.layer_norm_tlttdb) _input _normalized_shape _weight _bias _eps _cudnn_enable

native_layer_norm ::
  -- | input
  Tensor ->
  -- | normalized_shape
  [Int] ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | eps
  Double ->
  (Tensor, Tensor, Tensor)
native_layer_norm _input _normalized_shape _weight _bias _eps = unsafePerformIO $ (cast5 ATen.native_layer_norm_tlttd) _input _normalized_shape _weight _bias _eps

nan_to_num ::
  -- | self
  Tensor ->
  -- | nan
  Double ->
  -- | posinf
  Double ->
  -- | neginf
  Double ->
  Tensor
nan_to_num _self _nan _posinf _neginf = unsafePerformIO $ (cast4 ATen.nan_to_num_tddd) _self _nan _posinf _neginf

linear ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  Tensor
linear _input _weight _bias = unsafePerformIO $ (cast3 ATen.linear_ttt) _input _weight _bias

mkldnn_linear ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  Tensor
mkldnn_linear _self _weight _bias = unsafePerformIO $ (cast3 ATen.mkldnn_linear_ttt) _self _weight _bias

mkldnn_linear_backward_input ::
  -- | input_size
  [Int] ->
  -- | grad_output
  Tensor ->
  -- | weight
  Tensor ->
  Tensor
mkldnn_linear_backward_input _input_size _grad_output _weight = unsafePerformIO $ (cast3 ATen.mkldnn_linear_backward_input_ltt) _input_size _grad_output _weight

mkldnn_linear_backward_weights ::
  -- | grad_output
  Tensor ->
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias_defined
  Bool ->
  (Tensor, Tensor)
mkldnn_linear_backward_weights _grad_output _input _weight _bias_defined = unsafePerformIO $ (cast4 ATen.mkldnn_linear_backward_weights_tttb) _grad_output _input _weight _bias_defined

fbgemm_linear_int8_weight_fp32_activation ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | packed
  Tensor ->
  -- | col_offsets
  Tensor ->
  -- | weight_scale
  Float ->
  -- | weight_zero_point
  Float ->
  -- | bias
  Tensor ->
  Tensor
fbgemm_linear_int8_weight_fp32_activation _input _weight _packed _col_offsets _weight_scale _weight_zero_point _bias = unsafePerformIO $ (cast7 ATen.fbgemm_linear_int8_weight_fp32_activation_ttttsst) _input _weight _packed _col_offsets _weight_scale _weight_zero_point _bias

fbgemm_linear_int8_weight ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | packed
  Tensor ->
  -- | col_offsets
  Tensor ->
  -- | weight_scale
  Float ->
  -- | weight_zero_point
  Float ->
  -- | bias
  Tensor ->
  Tensor
fbgemm_linear_int8_weight _input _weight _packed _col_offsets _weight_scale _weight_zero_point _bias = unsafePerformIO $ (cast7 ATen.fbgemm_linear_int8_weight_ttttsst) _input _weight _packed _col_offsets _weight_scale _weight_zero_point _bias

fbgemm_linear_quantize_weight ::
  -- | input
  Tensor ->
  (Tensor, Tensor, Double, Int)
fbgemm_linear_quantize_weight _input = unsafePerformIO $ (cast1 ATen.fbgemm_linear_quantize_weight_t) _input

fbgemm_pack_gemm_matrix_fp16 ::
  -- | input
  Tensor ->
  Tensor
fbgemm_pack_gemm_matrix_fp16 _input = unsafePerformIO $ (cast1 ATen.fbgemm_pack_gemm_matrix_fp16_t) _input

fbgemm_linear_fp16_weight_fp32_activation ::
  -- | input
  Tensor ->
  -- | packed_weight
  Tensor ->
  -- | bias
  Tensor ->
  Tensor
fbgemm_linear_fp16_weight_fp32_activation _input _packed_weight _bias = unsafePerformIO $ (cast3 ATen.fbgemm_linear_fp16_weight_fp32_activation_ttt) _input _packed_weight _bias

fbgemm_linear_fp16_weight ::
  -- | input
  Tensor ->
  -- | packed_weight
  Tensor ->
  -- | bias
  Tensor ->
  Tensor
fbgemm_linear_fp16_weight _input _packed_weight _bias = unsafePerformIO $ (cast3 ATen.fbgemm_linear_fp16_weight_ttt) _input _packed_weight _bias

quantizeFbgemm' ::
  -- | input
  Tensor ->
  Tensor
quantizeFbgemm' _input = unsafePerformIO $ (cast1 ATen.fbgemm_pack_quantized_matrix_t) _input

quantizeFbgemm ::
  -- | input
  Tensor ->
  -- | K
  Int ->
  -- | N
  Int ->
  Tensor
quantizeFbgemm _input _K _N = unsafePerformIO $ (cast3 ATen.fbgemm_pack_quantized_matrix_tll) _input _K _N

ldexp ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
ldexp _self _other = unsafePerformIO $ (cast2 ATen.ldexp_tt) _self _other

log ::
  -- | self
  Tensor ->
  Tensor
log _self = unsafePerformIO $ (cast1 ATen.log_t) _self

log10 ::
  -- | self
  Tensor ->
  Tensor
log10 _self = unsafePerformIO $ (cast1 ATen.log10_t) _self

log1p ::
  -- | self
  Tensor ->
  Tensor
log1p _self = unsafePerformIO $ (cast1 ATen.log1p_t) _self

log2 ::
  -- | self
  Tensor ->
  Tensor
log2 _self = unsafePerformIO $ (cast1 ATen.log2_t) _self

logaddexp ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
logaddexp _self _other = unsafePerformIO $ (cast2 ATen.logaddexp_tt) _self _other

logaddexp2 ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
logaddexp2 _self _other = unsafePerformIO $ (cast2 ATen.logaddexp2_tt) _self _other

xlogy_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
xlogy_tt _self _other = unsafePerformIO $ (cast2 ATen.xlogy_tt) _self _other

xlogy_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
xlogy_st _self _other = unsafePerformIO $ (cast2 ATen.xlogy_st) _self _other

xlogy_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
xlogy_ts _self _other = unsafePerformIO $ (cast2 ATen.xlogy_ts) _self _other

logdet ::
  -- | self
  Tensor ->
  Tensor
logdet _self = unsafePerformIO $ (cast1 ATen.logdet_t) _self

logSoftmax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | dtype
  DType ->
  Tensor
logSoftmax _self _dim _dtype = unsafePerformIO $ (cast3 ATen.log_softmax_tls) _self _dim _dtype

logSoftmaxWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | dtype
  DType ->
  Tensor
logSoftmaxWithDimname _self _dim _dtype = unsafePerformIO $ (cast3 ATen.log_softmax_tns) _self _dim _dtype

logcumsumexp_tl ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Tensor
logcumsumexp_tl _self _dim = unsafePerformIO $ (cast2 ATen.logcumsumexp_tl) _self _dim

logcumsumexp_tn ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  Tensor
logcumsumexp_tn _self _dim = unsafePerformIO $ (cast2 ATen.logcumsumexp_tn) _self _dim

logsumexp ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
logsumexp _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.logsumexp_tlb) _self _dim _keepdim

logsumexpWithDimnameList ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | keepdim
  Bool ->
  Tensor
logsumexpWithDimnameList _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.logsumexp_tNb) _self _dim _keepdim

margin_ranking_loss ::
  -- | input1
  Tensor ->
  -- | input2
  Tensor ->
  -- | target
  Tensor ->
  -- | margin
  Double ->
  -- | reduction
  Int ->
  Tensor
margin_ranking_loss _input1 _input2 _target _margin _reduction = unsafePerformIO $ (cast5 ATen.margin_ranking_loss_tttdl) _input1 _input2 _target _margin _reduction

matmul ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
matmul _self _other = unsafePerformIO $ (cast2 ATen.matmul_tt) _self _other

matrixRank ::
  -- | self
  Tensor ->
  -- | tol
  Double ->
  -- | symmetric
  Bool ->
  Tensor
matrixRank _self _tol _symmetric = unsafePerformIO $ (cast3 ATen.matrix_rank_tdb) _self _tol _symmetric

matrixRank' ::
  -- | self
  Tensor ->
  -- | symmetric
  Bool ->
  Tensor
matrixRank' _self _symmetric = unsafePerformIO $ (cast2 ATen.matrix_rank_tb) _self _symmetric

matrix_power ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  Tensor
matrix_power _self _n = unsafePerformIO $ (cast2 ATen.matrix_power_tl) _self _n

matrix_exp ::
  -- | self
  Tensor ->
  Tensor
matrix_exp _self = unsafePerformIO $ (cast1 ATen.matrix_exp_t) _self

aminmax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
aminmax _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.aminmax_tlb) _self _dim _keepdim

maxDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
maxDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.max_tlb) _self _dim _keepdim

maxWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
maxWithDimname _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.max_tnb) _self _dim _keepdim

amax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
amax _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.amax_tlb) _self _dim _keepdim

max_pool1d_with_indices ::
  -- | self
  Tensor ->
  -- | kernel_size
  Int ->
  -- | stride
  Int ->
  -- | padding
  Int ->
  -- | dilation
  Int ->
  -- | ceil_mode
  Bool ->
  (Tensor, Tensor)
max_pool1d_with_indices _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.max_pool1d_with_indices_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

max_pool1d ::
  -- | self
  Tensor ->
  -- | kernel_size
  Int ->
  -- | stride
  Int ->
  -- | padding
  Int ->
  -- | dilation
  Int ->
  -- | ceil_mode
  Bool ->
  Tensor
max_pool1d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.max_pool1d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

max_pool2d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | ceil_mode
  Bool ->
  Tensor
max_pool2d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.max_pool2d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

mkldnn_max_pool2d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | ceil_mode
  Bool ->
  Tensor
mkldnn_max_pool2d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.mkldnn_max_pool2d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

mkldnn_max_pool3d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  -- | ceil_mode
  Bool ->
  Tensor
mkldnn_max_pool3d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.mkldnn_max_pool3d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

quantized_max_pool1d ::
  -- | self
  Tensor ->
  -- | kernel_size
  Int ->
  -- | stride
  Int ->
  -- | padding
  Int ->
  -- | dilation
  Int ->
  -- | ceil_mode
  Bool ->
  Tensor
quantized_max_pool1d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.quantized_max_pool1d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

quantized_max_pool2d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | ceil_mode
  Bool ->
  Tensor
quantized_max_pool2d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.quantized_max_pool2d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

max_pool3d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  -- | ceil_mode
  Bool ->
  Tensor
max_pool3d _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.max_pool3d_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

meanAll ::
  -- | self
  Tensor ->
  -- | dtype
  DType ->
  Tensor
meanAll _self _dtype = unsafePerformIO $ (cast2 ATen.mean_ts) _self _dtype

meanDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
meanDim _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.mean_tlbs) _self _dim _keepdim _dtype

meanWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
meanWithDimnames _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.mean_tNbs) _self _dim _keepdim _dtype

nanmean ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
nanmean _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.nanmean_tlbs) _self _dim _keepdim _dtype

medianAll ::
  -- | self
  Tensor ->
  Tensor
medianAll _self = unsafePerformIO $ (cast1 ATen.median_t) _self

medianDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
medianDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.median_tlb) _self _dim _keepdim

medianWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
medianWithDimname _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.median_tnb) _self _dim _keepdim

nanmedian_t ::
  -- | self
  Tensor ->
  Tensor
nanmedian_t _self = unsafePerformIO $ (cast1 ATen.nanmedian_t) _self

nanmedian_tlb ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
nanmedian_tlb _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.nanmedian_tlb) _self _dim _keepdim

nanmedian_tnb ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
nanmedian_tnb _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.nanmedian_tnb) _self _dim _keepdim

minDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
minDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.min_tlb) _self _dim _keepdim

minWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
minWithDimname _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.min_tnb) _self _dim _keepdim

amin ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
amin _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.amin_tlb) _self _dim _keepdim

mkldnn_convolution ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | padding
  [Int] ->
  -- | stride
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  Tensor
mkldnn_convolution _self _weight _bias _padding _stride _dilation _groups = unsafePerformIO $ (cast7 ATen.mkldnn_convolution_tttllll) _self _weight _bias _padding _stride _dilation _groups

miopen_batch_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | training
  Bool ->
  -- | exponential_average_factor
  Double ->
  -- | epsilon
  Double ->
  (Tensor, Tensor, Tensor)
miopen_batch_norm _input _weight _bias _running_mean _running_var _training _exponential_average_factor _epsilon = unsafePerformIO $ (cast8 ATen.miopen_batch_norm_tttttbdd) _input _weight _bias _running_mean _running_var _training _exponential_average_factor _epsilon

miopen_convolution ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | padding
  [Int] ->
  -- | stride
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  -- | benchmark
  Bool ->
  -- | deterministic
  Bool ->
  Tensor
miopen_convolution _self _weight _bias _padding _stride _dilation _groups _benchmark _deterministic = unsafePerformIO $ (cast9 ATen.miopen_convolution_tttllllbb) _self _weight _bias _padding _stride _dilation _groups _benchmark _deterministic

miopen_convolution_transpose ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | padding
  [Int] ->
  -- | output_padding
  [Int] ->
  -- | stride
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  -- | benchmark
  Bool ->
  -- | deterministic
  Bool ->
  Tensor
miopen_convolution_transpose _self _weight _bias _padding _output_padding _stride _dilation _groups _benchmark _deterministic = unsafePerformIO $ (cast10 ATen.miopen_convolution_transpose_tttlllllbb) _self _weight _bias _padding _output_padding _stride _dilation _groups _benchmark _deterministic

miopen_depthwise_convolution ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | padding
  [Int] ->
  -- | stride
  [Int] ->
  -- | dilation
  [Int] ->
  -- | groups
  Int ->
  -- | benchmark
  Bool ->
  -- | deterministic
  Bool ->
  Tensor
miopen_depthwise_convolution _self _weight _bias _padding _stride _dilation _groups _benchmark _deterministic = unsafePerformIO $ (cast9 ATen.miopen_depthwise_convolution_tttllllbb) _self _weight _bias _padding _stride _dilation _groups _benchmark _deterministic

miopen_rnn ::
  -- | input
  Tensor ->
  -- | weight
  [Tensor] ->
  -- | weight_stride0
  Int ->
  -- | hx
  Tensor ->
  -- | cx
  Tensor ->
  -- | mode
  Int ->
  -- | hidden_size
  Int ->
  -- | num_layers
  Int ->
  -- | batch_first
  Bool ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  -- | batch_sizes
  [Int] ->
  -- | dropout_state
  Tensor ->
  (Tensor, Tensor, Tensor, Tensor, Tensor)
miopen_rnn _input _weight _weight_stride0 _hx _cx _mode _hidden_size _num_layers _batch_first _dropout _train _bidirectional _batch_sizes _dropout_state = unsafePerformIO $ (cast14 ATen.miopen_rnn_tllttlllbdbblt) _input _weight _weight_stride0 _hx _cx _mode _hidden_size _num_layers _batch_first _dropout _train _bidirectional _batch_sizes _dropout_state

mm ::
  -- | self
  Tensor ->
  -- | mat2
  Tensor ->
  Tensor
mm _self _mat2 = unsafePerformIO $ (cast2 ATen.mm_tt) _self _mat2

mode ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
mode _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.mode_tlb) _self _dim _keepdim

modeWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
modeWithDimname _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.mode_tnb) _self _dim _keepdim

mul ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
mul _self _other = unsafePerformIO $ (cast2 ATen.mul_tt) _self _other

mulScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
mulScalar _self _other = unsafePerformIO $ (cast2 ATen.mul_ts) _self _other

multiply_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
multiply_tt _self _other = unsafePerformIO $ (cast2 ATen.multiply_tt) _self _other

multiply_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
multiply_ts _self _other = unsafePerformIO $ (cast2 ATen.multiply_ts) _self _other

mv ::
  -- | self
  Tensor ->
  -- | vec
  Tensor ->
  Tensor
mv _self _vec = unsafePerformIO $ (cast2 ATen.mv_tt) _self _vec

mvlgamma ::
  -- | self
  Tensor ->
  -- | p
  Int ->
  Tensor
mvlgamma _self _p = unsafePerformIO $ (cast2 ATen.mvlgamma_tl) _self _p

narrow_copy ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | start
  Int ->
  -- | length
  Int ->
  Tensor
narrow_copy _self _dim _start _length = unsafePerformIO $ (cast4 ATen.narrow_copy_tlll) _self _dim _start _length

narrow_tlll ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | start
  Int ->
  -- | length
  Int ->
  Tensor
narrow_tlll _self _dim _start _length = unsafePerformIO $ (cast4 ATen.narrow_tlll) _self _dim _start _length

narrow_tltl ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | start
  Tensor ->
  -- | length
  Int ->
  Tensor
narrow_tltl _self _dim _start _length = unsafePerformIO $ (cast4 ATen.narrow_tltl) _self _dim _start _length

native_batch_norm ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | training
  Bool ->
  -- | momentum
  Double ->
  -- | eps
  Double ->
  (Tensor, Tensor, Tensor)
native_batch_norm _input _weight _bias _running_mean _running_var _training _momentum _eps = unsafePerformIO $ (cast8 ATen.native_batch_norm_tttttbdd) _input _weight _bias _running_mean _running_var _training _momentum _eps

batch_norm_stats ::
  -- | input
  Tensor ->
  -- | eps
  Double ->
  (Tensor, Tensor)
batch_norm_stats _input _eps = unsafePerformIO $ (cast2 ATen.batch_norm_stats_td) _input _eps

batch_norm_elemt ::
  -- | input
  Tensor ->
  -- | weight
  Tensor ->
  -- | bias
  Tensor ->
  -- | mean
  Tensor ->
  -- | invstd
  Tensor ->
  -- | eps
  Double ->
  Tensor
batch_norm_elemt _input _weight _bias _mean _invstd _eps = unsafePerformIO $ (cast6 ATen.batch_norm_elemt_tttttd) _input _weight _bias _mean _invstd _eps

batch_norm_gather_stats ::
  -- | input
  Tensor ->
  -- | mean
  Tensor ->
  -- | invstd
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | momentum
  Double ->
  -- | eps
  Double ->
  -- | count
  Int ->
  (Tensor, Tensor)
batch_norm_gather_stats _input _mean _invstd _running_mean _running_var _momentum _eps _count = unsafePerformIO $ (cast8 ATen.batch_norm_gather_stats_tttttddl) _input _mean _invstd _running_mean _running_var _momentum _eps _count

batch_norm_gather_stats_with_counts ::
  -- | input
  Tensor ->
  -- | mean
  Tensor ->
  -- | invstd
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | momentum
  Double ->
  -- | eps
  Double ->
  -- | counts
  Tensor ->
  (Tensor, Tensor)
batch_norm_gather_stats_with_counts _input _mean _invstd _running_mean _running_var _momentum _eps _counts = unsafePerformIO $ (cast8 ATen.batch_norm_gather_stats_with_counts_tttttddt) _input _mean _invstd _running_mean _running_var _momentum _eps _counts

batch_norm_backward_reduce ::
  -- | grad_out
  Tensor ->
  -- | input
  Tensor ->
  -- | mean
  Tensor ->
  -- | invstd
  Tensor ->
  -- | weight
  Tensor ->
  -- | input_g
  Bool ->
  -- | weight_g
  Bool ->
  -- | bias_g
  Bool ->
  (Tensor, Tensor, Tensor, Tensor)
batch_norm_backward_reduce _grad_out _input _mean _invstd _weight _input_g _weight_g _bias_g = unsafePerformIO $ (cast8 ATen.batch_norm_backward_reduce_tttttbbb) _grad_out _input _mean _invstd _weight _input_g _weight_g _bias_g

batch_norm_backward_elemt ::
  -- | grad_out
  Tensor ->
  -- | input
  Tensor ->
  -- | mean
  Tensor ->
  -- | invstd
  Tensor ->
  -- | weight
  Tensor ->
  -- | mean_dy
  Tensor ->
  -- | mean_dy_xmu
  Tensor ->
  -- | count
  Tensor ->
  Tensor
batch_norm_backward_elemt _grad_out _input _mean _invstd _weight _mean_dy _mean_dy_xmu _count = unsafePerformIO $ (cast8 ATen.batch_norm_backward_elemt_tttttttt) _grad_out _input _mean _invstd _weight _mean_dy _mean_dy_xmu _count

batch_norm_update_stats ::
  -- | input
  Tensor ->
  -- | running_mean
  Tensor ->
  -- | running_var
  Tensor ->
  -- | momentum
  Double ->
  (Tensor, Tensor)
batch_norm_update_stats _input _running_mean _running_var _momentum = unsafePerformIO $ (cast4 ATen.batch_norm_update_stats_tttd) _input _running_mean _running_var _momentum

is_vulkan_available ::
  Bool
is_vulkan_available = unsafePerformIO $ (cast0 ATen.is_vulkan_available)

pairwise_distance ::
  -- | x1
  Tensor ->
  -- | x2
  Tensor ->
  -- | p
  Double ->
  -- | eps
  Double ->
  -- | keepdim
  Bool ->
  Tensor
pairwise_distance _x1 _x2 _p _eps _keepdim = unsafePerformIO $ (cast5 ATen.pairwise_distance_ttddb) _x1 _x2 _p _eps _keepdim

cdist ::
  -- | x1
  Tensor ->
  -- | x2
  Tensor ->
  -- | p
  Double ->
  -- | compute_mode
  Int ->
  Tensor
cdist _x1 _x2 _p _compute_mode = unsafePerformIO $ (cast4 ATen.cdist_ttdl) _x1 _x2 _p _compute_mode

pdist ::
  -- | self
  Tensor ->
  -- | p
  Double ->
  Tensor
pdist _self _p = unsafePerformIO $ (cast2 ATen.pdist_td) _self _p

cosine_similarity ::
  -- | x1
  Tensor ->
  -- | x2
  Tensor ->
  -- | dim
  Int ->
  -- | eps
  Double ->
  Tensor
cosine_similarity _x1 _x2 _dim _eps = unsafePerformIO $ (cast4 ATen.cosine_similarity_ttld) _x1 _x2 _dim _eps

permute ::
  -- | self
  Tensor ->
  -- | dims
  [Int] ->
  Tensor
permute _self _dims = unsafePerformIO $ (cast2 ATen.permute_tl) _self _dims

movedim_tll ::
  -- | self
  Tensor ->
  -- | source
  [Int] ->
  -- | destination
  [Int] ->
  Tensor
movedim_tll _self _source _destination = unsafePerformIO $ (cast3 ATen.movedim_tll) _self _source _destination

-- movedim_tll
--   :: Tensor -- ^ self
--   -> Int -- ^ source
--   -> Int -- ^ destination
--   -> Tensor
-- movedim_tll _self _source _destination = unsafePerformIO $ (cast3 ATen.movedim_tll) _self _source _destination

moveaxis_tll ::
  -- | self
  Tensor ->
  -- | source
  [Int] ->
  -- | destination
  [Int] ->
  Tensor
moveaxis_tll _self _source _destination = unsafePerformIO $ (cast3 ATen.moveaxis_tll) _self _source _destination

-- moveaxis_tll
--   :: Tensor -- ^ self
--   -> Int -- ^ source
--   -> Int -- ^ destination
--   -> Tensor
-- moveaxis_tll _self _source _destination = unsafePerformIO $ (cast3 ATen.moveaxis_tll) _self _source _destination

adjoint ::
  -- | self
  Tensor ->
  Tensor
adjoint _self = unsafePerformIO $ (cast1 ATen.adjoint_t) _self

pixel_shuffle ::
  -- | self
  Tensor ->
  -- | upscale_factor
  Int ->
  Tensor
pixel_shuffle _self _upscale_factor = unsafePerformIO $ (cast2 ATen.pixel_shuffle_tl) _self _upscale_factor

pixel_unshuffle ::
  -- | self
  Tensor ->
  -- | downscale_factor
  Int ->
  Tensor
pixel_unshuffle _self _downscale_factor = unsafePerformIO $ (cast2 ATen.pixel_unshuffle_tl) _self _downscale_factor

channel_shuffle ::
  -- | self
  Tensor ->
  -- | groups
  Int ->
  Tensor
channel_shuffle _self _groups = unsafePerformIO $ (cast2 ATen.channel_shuffle_tl) _self _groups

native_channel_shuffle ::
  -- | self
  Tensor ->
  -- | groups
  Int ->
  Tensor
native_channel_shuffle _self _groups = unsafePerformIO $ (cast2 ATen.native_channel_shuffle_tl) _self _groups

pinverse ::
  -- | self
  Tensor ->
  -- | rcond
  Double ->
  Tensor
pinverse _self _rcond = unsafePerformIO $ (cast2 ATen.pinverse_td) _self _rcond

poisson_nll_loss ::
  -- | input
  Tensor ->
  -- | target
  Tensor ->
  -- | log_input
  Bool ->
  -- | full
  Bool ->
  -- | eps
  Double ->
  -- | reduction
  Int ->
  Tensor
poisson_nll_loss _input _target _log_input _full _eps _reduction = unsafePerformIO $ (cast6 ATen.poisson_nll_loss_ttbbdl) _input _target _log_input _full _eps _reduction

rad2deg ::
  -- | self
  Tensor ->
  Tensor
rad2deg _self = unsafePerformIO $ (cast1 ATen.rad2deg_t) _self

deg2rad ::
  -- | self
  Tensor ->
  Tensor
deg2rad _self = unsafePerformIO $ (cast1 ATen.deg2rad_t) _self

ravel ::
  -- | self
  Tensor ->
  Tensor
ravel _self = unsafePerformIO $ (cast1 ATen.ravel_t) _self

reciprocal ::
  -- | self
  Tensor ->
  Tensor
reciprocal _self = unsafePerformIO $ (cast1 ATen.reciprocal_t) _self

neg ::
  -- | self
  Tensor ->
  Tensor
neg _self = unsafePerformIO $ (cast1 ATen.neg_t) _self

negative ::
  -- | self
  Tensor ->
  Tensor
negative _self = unsafePerformIO $ (cast1 ATen.negative_t) _self

repeat_interleave_tl ::
  -- | repeats
  Tensor ->
  -- | output_size
  Int ->
  Tensor
repeat_interleave_tl _repeats _output_size = unsafePerformIO $ (cast2 ATen.repeat_interleave_tl) _repeats _output_size

repeat_interleave_ttll ::
  -- | self
  Tensor ->
  -- | repeats
  Tensor ->
  -- | dim
  Int ->
  -- | output_size
  Int ->
  Tensor
repeat_interleave_ttll _self _repeats _dim _output_size = unsafePerformIO $ (cast4 ATen.repeat_interleave_ttll) _self _repeats _dim _output_size

repeat_interleave_tlll ::
  -- | self
  Tensor ->
  -- | repeats
  Int ->
  -- | dim
  Int ->
  -- | output_size
  Int ->
  Tensor
repeat_interleave_tlll _self _repeats _dim _output_size = unsafePerformIO $ (cast4 ATen.repeat_interleave_tlll) _self _repeats _dim _output_size

reshape ::
  -- | self
  Tensor ->
  -- | shape
  [Int] ->
  Tensor
reshape _self _shape = unsafePerformIO $ (cast2 ATen.reshape_tl) _self _shape

round_t ::
  -- | self
  Tensor ->
  Tensor
round_t _self = unsafePerformIO $ (cast1 ATen.round_t) _self

round_tl ::
  -- | self
  Tensor ->
  -- | decimals
  Int ->
  Tensor
round_tl _self _decimals = unsafePerformIO $ (cast2 ATen.round_tl) _self _decimals

relu ::
  -- | self
  Tensor ->
  Tensor
relu _self = unsafePerformIO $ (cast1 ATen.relu_t) _self

relu6 ::
  -- | self
  Tensor ->
  Tensor
relu6 _self = unsafePerformIO $ (cast1 ATen.relu6_t) _self

prelu ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  Tensor
prelu _self _weight = unsafePerformIO $ (cast2 ATen.prelu_tt) _self _weight

gelu ::
  -- | self
  Tensor ->
  Tensor
gelu _self = unsafePerformIO $ (cast1 ATen.gelu_t) _self

hardshrink ::
  -- | self
  Tensor ->
  -- | lambd
  Float ->
  Tensor
hardshrink _self _lambd = unsafePerformIO $ (cast2 ATen.hardshrink_ts) _self _lambd

rsqrt ::
  -- | self
  Tensor ->
  Tensor
rsqrt _self = unsafePerformIO $ (cast1 ATen.rsqrt_t) _self

selectWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Int ->
  Tensor
selectWithDimname _self _dim _index = unsafePerformIO $ (cast3 ATen.select_tnl) _self _dim _index

select ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Int ->
  Tensor
select _self _dim _index = unsafePerformIO $ (cast3 ATen.select_tll) _self _dim _index

selu ::
  -- | self
  Tensor ->
  Tensor
selu _self = unsafePerformIO $ (cast1 ATen.selu_t) _self

celu ::
  -- | self
  Tensor ->
  -- | alpha
  Float ->
  Tensor
celu _self _alpha = unsafePerformIO $ (cast2 ATen.celu_ts) _self _alpha

silu ::
  -- | self
  Tensor ->
  Tensor
silu _self = unsafePerformIO $ (cast1 ATen.silu_t) _self

mish ::
  -- | self
  Tensor ->
  Tensor
mish _self = unsafePerformIO $ (cast1 ATen.mish_t) _self

sigmoid ::
  -- | self
  Tensor ->
  Tensor
sigmoid _self = unsafePerformIO $ (cast1 ATen.sigmoid_t) _self

logit ::
  -- | self
  Tensor ->
  -- | eps
  Double ->
  Tensor
logit _self _eps = unsafePerformIO $ (cast2 ATen.logit_td) _self _eps

sin ::
  -- | self
  Tensor ->
  Tensor
sin _self = unsafePerformIO $ (cast1 ATen.sin_t) _self

sinc ::
  -- | self
  Tensor ->
  Tensor
sinc _self = unsafePerformIO $ (cast1 ATen.sinc_t) _self

sinh ::
  -- | self
  Tensor ->
  Tensor
sinh _self = unsafePerformIO $ (cast1 ATen.sinh_t) _self

size ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Int
size _self _dim = unsafePerformIO $ (cast2 ATen.size_tl) _self _dim

sizeWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  Int
sizeWithDimname _self _dim = unsafePerformIO $ (cast2 ATen.size_tn) _self _dim

slice ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | start
  Int ->
  -- | end
  Int ->
  -- | step
  Int ->
  Tensor
slice _self _dim _start _end _step = unsafePerformIO $ (cast5 ATen.slice_tllll) _self _dim _start _end _step

slice_scatter ::
  -- | self
  Tensor ->
  -- | src
  Tensor ->
  -- | dim
  Int ->
  -- | start
  Int ->
  -- | end
  Int ->
  -- | step
  Int ->
  Tensor
slice_scatter _self _src _dim _start _end _step = unsafePerformIO $ (cast6 ATen.slice_scatter_ttllll) _self _src _dim _start _end _step

select_scatter ::
  -- | self
  Tensor ->
  -- | src
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Int ->
  Tensor
select_scatter _self _src _dim _index = unsafePerformIO $ (cast4 ATen.select_scatter_ttll) _self _src _dim _index

diagonal_scatter ::
  -- | self
  Tensor ->
  -- | src
  Tensor ->
  -- | offset
  Int ->
  -- | dim1
  Int ->
  -- | dim2
  Int ->
  Tensor
diagonal_scatter _self _src _offset _dim1 _dim2 = unsafePerformIO $ (cast5 ATen.diagonal_scatter_ttlll) _self _src _offset _dim1 _dim2

slogdet ::
  -- | self
  Tensor ->
  (Tensor, Tensor)
slogdet _self = unsafePerformIO $ (cast1 ATen.slogdet_t) _self

smm ::
  -- | self
  Tensor ->
  -- | mat2
  Tensor ->
  Tensor
smm _self _mat2 = unsafePerformIO $ (cast2 ATen.smm_tt) _self _mat2

softmax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | dtype
  DType ->
  Tensor
softmax _self _dim _dtype = unsafePerformIO $ (cast3 ATen.softmax_tls) _self _dim _dtype

softmaxWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | dtype
  DType ->
  Tensor
softmaxWithDimname _self _dim _dtype = unsafePerformIO $ (cast3 ATen.softmax_tns) _self _dim _dtype

unsafe_split ::
  -- | self
  Tensor ->
  -- | split_size
  Int ->
  -- | dim
  Int ->
  [Tensor]
unsafe_split _self _split_size _dim = unsafePerformIO $ (cast3 ATen.unsafe_split_tll) _self _split_size _dim

split ::
  -- | self
  Tensor ->
  -- | split_size
  Int ->
  -- | dim
  Int ->
  [Tensor]
split _self _split_size _dim = unsafePerformIO $ (cast3 ATen.split_tll) _self _split_size _dim

unsafe_split_with_sizes ::
  -- | self
  Tensor ->
  -- | split_sizes
  [Int] ->
  -- | dim
  Int ->
  [Tensor]
unsafe_split_with_sizes _self _split_sizes _dim = unsafePerformIO $ (cast3 ATen.unsafe_split_with_sizes_tll) _self _split_sizes _dim

split_with_sizes ::
  -- | self
  Tensor ->
  -- | split_sizes
  [Int] ->
  -- | dim
  Int ->
  [Tensor]
split_with_sizes _self _split_sizes _dim = unsafePerformIO $ (cast3 ATen.split_with_sizes_tll) _self _split_sizes _dim

hsplit_tl ::
  -- | self
  Tensor ->
  -- | sections
  Int ->
  [Tensor]
hsplit_tl _self _sections = unsafePerformIO $ (cast2 ATen.hsplit_tl) _self _sections

-- hsplit_tl
--   :: Tensor -- ^ self
--   -> [Int] -- ^ indices
--   -> [Tensor]
-- hsplit_tl _self _indices = unsafePerformIO $ (cast2 ATen.hsplit_tl) _self _indices

vsplit_tl ::
  -- | self
  Tensor ->
  -- | sections
  Int ->
  [Tensor]
vsplit_tl _self _sections = unsafePerformIO $ (cast2 ATen.vsplit_tl) _self _sections

-- vsplit_tl
--   :: Tensor -- ^ self
--   -> [Int] -- ^ indices
--   -> [Tensor]
-- vsplit_tl _self _indices = unsafePerformIO $ (cast2 ATen.vsplit_tl) _self _indices

dsplit_tl ::
  -- | self
  Tensor ->
  -- | sections
  Int ->
  [Tensor]
dsplit_tl _self _sections = unsafePerformIO $ (cast2 ATen.dsplit_tl) _self _sections

-- dsplit_tl
--   :: Tensor -- ^ self
--   -> [Int] -- ^ indices
--   -> [Tensor]
-- dsplit_tl _self _indices = unsafePerformIO $ (cast2 ATen.dsplit_tl) _self _indices

squeezeAll ::
  -- | self
  Tensor ->
  Tensor
squeezeAll _self = unsafePerformIO $ (cast1 ATen.squeeze_t) _self

squeezeDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Tensor
squeezeDim _self _dim = unsafePerformIO $ (cast2 ATen.squeeze_tl) _self _dim

squeezeWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  Tensor
squeezeWithDimname _self _dim = unsafePerformIO $ (cast2 ATen.squeeze_tn) _self _dim

sspaddmm ::
  -- | self
  Tensor ->
  -- | mat1
  Tensor ->
  -- | mat2
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
sspaddmm _self _mat1 _mat2 _beta _alpha = unsafePerformIO $ (cast5 ATen.sspaddmm_tttss) _self _mat1 _mat2 _beta _alpha

stack ::
  -- | tensors
  [Tensor] ->
  -- | dim
  Int ->
  Tensor
stack _tensors _dim = unsafePerformIO $ (cast2 ATen.stack_ll) _tensors _dim

hstack ::
  -- | tensors
  [Tensor] ->
  Tensor
hstack _tensors = unsafePerformIO $ (cast1 ATen.hstack_l) _tensors

vstack ::
  -- | tensors
  [Tensor] ->
  Tensor
vstack _tensors = unsafePerformIO $ (cast1 ATen.vstack_l) _tensors

dstack ::
  -- | tensors
  [Tensor] ->
  Tensor
dstack _tensors = unsafePerformIO $ (cast1 ATen.dstack_l) _tensors

stft ::
  -- | self
  Tensor ->
  -- | n_fft
  Int ->
  -- | hop_length
  Int ->
  -- | win_length
  Int ->
  -- | window
  Tensor ->
  -- | normalized
  Bool ->
  -- | onesided
  Bool ->
  -- | return_complex
  Bool ->
  Tensor
stft _self _n_fft _hop_length _win_length _window _normalized _onesided _return_complex = unsafePerformIO $ (cast8 ATen.stft_tllltbbb) _self _n_fft _hop_length _win_length _window _normalized _onesided _return_complex

istft ::
  -- | self
  Tensor ->
  -- | n_fft
  Int ->
  -- | hop_length
  Int ->
  -- | win_length
  Int ->
  -- | window
  Tensor ->
  -- | center
  Bool ->
  -- | normalized
  Bool ->
  -- | onesided
  Bool ->
  -- | length
  Int ->
  -- | return_complex
  Bool ->
  Tensor
istft _self _n_fft _hop_length _win_length _window _center _normalized _onesided _length _return_complex = unsafePerformIO $ (cast10 ATen.istft_tllltbbblb) _self _n_fft _hop_length _win_length _window _center _normalized _onesided _length _return_complex

stride ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Int
stride _self _dim = unsafePerformIO $ (cast2 ATen.stride_tl) _self _dim

strideWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  Int
strideWithDimname _self _dim = unsafePerformIO $ (cast2 ATen.stride_tn) _self _dim

sumAll ::
  -- | self
  Tensor ->
  -- | dtype
  DType ->
  Tensor
sumAll _self _dtype = unsafePerformIO $ (cast2 ATen.sum_ts) _self _dtype

sumDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
sumDim _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.sum_tlbs) _self _dim _keepdim _dtype

sumWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
sumWithDimnames _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.sum_tNbs) _self _dim _keepdim _dtype

nansum_ts ::
  -- | self
  Tensor ->
  -- | dtype
  DType ->
  Tensor
nansum_ts _self _dtype = unsafePerformIO $ (cast2 ATen.nansum_ts) _self _dtype

nansum_tlbs ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
nansum_tlbs _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.nansum_tlbs) _self _dim _keepdim _dtype

sqrt ::
  -- | self
  Tensor ->
  Tensor
sqrt _self = unsafePerformIO $ (cast1 ATen.sqrt_t) _self

square ::
  -- | self
  Tensor ->
  Tensor
square _self = unsafePerformIO $ (cast1 ATen.square_t) _self

stdAll ::
  -- | self
  Tensor ->
  -- | unbiased
  Bool ->
  Tensor
stdAll _self _unbiased = unsafePerformIO $ (cast2 ATen.std_tb) _self _unbiased

stdDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  Tensor
stdDim _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.std_tlbb) _self _dim _unbiased _keepdim

std_tllb ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  Tensor
std_tllb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.std_tllb) _self _dim _correction _keepdim

stdMeanAll ::
  -- | self
  Tensor ->
  -- | unbiased
  Bool ->
  (Tensor, Tensor)
stdMeanAll _self _unbiased = unsafePerformIO $ (cast2 ATen.std_mean_tb) _self _unbiased

stdMeanDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
stdMeanDim _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.std_mean_tlbb) _self _dim _unbiased _keepdim

std_mean_tllb ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
std_mean_tllb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.std_mean_tllb) _self _dim _correction _keepdim

stdMeanWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
stdMeanWithDimnames _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.std_mean_tNbb) _self _dim _unbiased _keepdim

std_mean_tNlb ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
std_mean_tNlb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.std_mean_tNlb) _self _dim _correction _keepdim

stdWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  Tensor
stdWithDimnames _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.std_tNbb) _self _dim _unbiased _keepdim

std_tNlb ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  Tensor
std_tNlb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.std_tNlb) _self _dim _correction _keepdim

prodAll ::
  -- | self
  Tensor ->
  -- | dtype
  DType ->
  Tensor
prodAll _self _dtype = unsafePerformIO $ (cast2 ATen.prod_ts) _self _dtype

prodDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
prodDim _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.prod_tlbs) _self _dim _keepdim _dtype

prodWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
prodWithDimnames _self _dim _keepdim _dtype = unsafePerformIO $ (cast4 ATen.prod_tnbs) _self _dim _keepdim _dtype

t ::
  -- | self
  Tensor ->
  Tensor
t _self = unsafePerformIO $ (cast1 ATen.t_t) _self

tan ::
  -- | self
  Tensor ->
  Tensor
tan _self = unsafePerformIO $ (cast1 ATen.tan_t) _self

tanh ::
  -- | self
  Tensor ->
  Tensor
tanh _self = unsafePerformIO $ (cast1 ATen.tanh_t) _self

tensordot ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | dims_self
  [Int] ->
  -- | dims_other
  [Int] ->
  Tensor
tensordot _self _other _dims_self _dims_other = unsafePerformIO $ (cast4 ATen.tensordot_ttll) _self _other _dims_self _dims_other

threshold ::
  -- | self
  Tensor ->
  -- | threshold
  Float ->
  -- | value
  Float ->
  Tensor
threshold _self _threshold _value = unsafePerformIO $ (cast3 ATen.threshold_tss) _self _threshold _value

tile ::
  -- | self
  Tensor ->
  -- | dims
  [Int] ->
  Tensor
tile _self _dims = unsafePerformIO $ (cast2 ATen.tile_tl) _self _dims

transpose ::
  -- | self
  Tensor ->
  -- | dim0
  Int ->
  -- | dim1
  Int ->
  Tensor
transpose _self _dim0 _dim1 = unsafePerformIO $ (cast3 ATen.transpose_tll) _self _dim0 _dim1

transposeWithDimname ::
  -- | self
  Tensor ->
  -- | dim0
  Dimname ->
  -- | dim1
  Dimname ->
  Tensor
transposeWithDimname _self _dim0 _dim1 = unsafePerformIO $ (cast3 ATen.transpose_tnn) _self _dim0 _dim1

one_hot ::
  -- | self
  Tensor ->
  -- | num_classes
  Int ->
  Tensor
one_hot _self _num_classes = unsafePerformIO $ (cast2 ATen.one_hot_tl) _self _num_classes

flip ::
  -- | self
  Tensor ->
  -- | dims
  [Int] ->
  Tensor
flip _self _dims = unsafePerformIO $ (cast2 ATen.flip_tl) _self _dims

fliplr ::
  -- | self
  Tensor ->
  Tensor
fliplr _self = unsafePerformIO $ (cast1 ATen.fliplr_t) _self

flipud ::
  -- | self
  Tensor ->
  Tensor
flipud _self = unsafePerformIO $ (cast1 ATen.flipud_t) _self

roll ::
  -- | self
  Tensor ->
  -- | shifts
  Int ->
  -- | dims
  Int ->
  Tensor
roll _self _shifts _dims = unsafePerformIO $ (cast3 ATen.roll_tll) _self _shifts _dims

rot90 ::
  -- | self
  Tensor ->
  -- | k
  Int ->
  -- | dims
  [Int] ->
  Tensor
rot90 _self _k _dims = unsafePerformIO $ (cast3 ATen.rot90_tll) _self _k _dims

trapezoid_ttl ::
  -- | y
  Tensor ->
  -- | x
  Tensor ->
  -- | dim
  Int ->
  Tensor
trapezoid_ttl _y _x _dim = unsafePerformIO $ (cast3 ATen.trapezoid_ttl) _y _x _dim

trapezoid_tsl ::
  -- | y
  Tensor ->
  -- | dx
  Float ->
  -- | dim
  Int ->
  Tensor
trapezoid_tsl _y _dx _dim = unsafePerformIO $ (cast3 ATen.trapezoid_tsl) _y _dx _dim

trapz ::
  -- | y
  Tensor ->
  -- | x
  Tensor ->
  -- | dim
  Int ->
  Tensor
trapz _y _x _dim = unsafePerformIO $ (cast3 ATen.trapz_ttl) _y _x _dim

trapzScalar ::
  -- | y
  Tensor ->
  -- | dx
  Double ->
  -- | dim
  Int ->
  Tensor
trapzScalar _y _dx _dim = unsafePerformIO $ (cast3 ATen.trapz_tdl) _y _dx _dim

triplet_margin_loss ::
  -- | anchor
  Tensor ->
  -- | positive
  Tensor ->
  -- | negative
  Tensor ->
  -- | margin
  Double ->
  -- | p
  Double ->
  -- | eps
  Double ->
  -- | swap
  Bool ->
  -- | reduction
  Int ->
  Tensor
triplet_margin_loss _anchor _positive _negative _margin _p _eps _swap _reduction = unsafePerformIO $ (cast8 ATen.triplet_margin_loss_tttdddbl) _anchor _positive _negative _margin _p _eps _swap _reduction

trunc ::
  -- | self
  Tensor ->
  Tensor
trunc _self = unsafePerformIO $ (cast1 ATen.trunc_t) _self

fix ::
  -- | self
  Tensor ->
  Tensor
fix _self = unsafePerformIO $ (cast1 ATen.fix_t) _self

unique_dim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | sorted
  Bool ->
  -- | return_inverse
  Bool ->
  -- | return_counts
  Bool ->
  (Tensor, Tensor, Tensor)
unique_dim _self _dim _sorted _return_inverse _return_counts = unsafePerformIO $ (cast5 ATen.unique_dim_tlbbb) _self _dim _sorted _return_inverse _return_counts

unique_consecutive ::
  -- | self
  Tensor ->
  -- | return_inverse
  Bool ->
  -- | return_counts
  Bool ->
  -- | dim
  Int ->
  (Tensor, Tensor, Tensor)
unique_consecutive _self _return_inverse _return_counts _dim = unsafePerformIO $ (cast4 ATen.unique_consecutive_tbbl) _self _return_inverse _return_counts _dim

unique_dim_consecutive ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | return_inverse
  Bool ->
  -- | return_counts
  Bool ->
  (Tensor, Tensor, Tensor)
unique_dim_consecutive _self _dim _return_inverse _return_counts = unsafePerformIO $ (cast4 ATen.unique_dim_consecutive_tlbb) _self _dim _return_inverse _return_counts

unsqueeze ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Tensor
unsqueeze _self _dim = unsafePerformIO $ (cast2 ATen.unsqueeze_tl) _self _dim

vander ::
  -- | x
  Tensor ->
  -- | N
  Int ->
  -- | increasing
  Bool ->
  Tensor
vander _x _N _increasing = unsafePerformIO $ (cast3 ATen.vander_tlb) _x _N _increasing

var ::
  -- | self
  Tensor ->
  -- | unbiased
  Bool ->
  Tensor
var _self _unbiased = unsafePerformIO $ (cast2 ATen.var_tb) _self _unbiased

varDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  Tensor
varDim _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.var_tlbb) _self _dim _unbiased _keepdim

var_tllb ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  Tensor
var_tllb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.var_tllb) _self _dim _correction _keepdim

varWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  Tensor
varWithDimnames _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.var_tNbb) _self _dim _unbiased _keepdim

var_tNlb ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  Tensor
var_tNlb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.var_tNlb) _self _dim _correction _keepdim

varMean ::
  -- | self
  Tensor ->
  -- | unbiased
  Bool ->
  (Tensor, Tensor)
varMean _self _unbiased = unsafePerformIO $ (cast2 ATen.var_mean_tb) _self _unbiased

varMeanDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
varMeanDim _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.var_mean_tlbb) _self _dim _unbiased _keepdim

var_mean_tllb ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
var_mean_tllb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.var_mean_tllb) _self _dim _correction _keepdim

varMeanWithDimnames ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | unbiased
  Bool ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
varMeanWithDimnames _self _dim _unbiased _keepdim = unsafePerformIO $ (cast4 ATen.var_mean_tNbb) _self _dim _unbiased _keepdim

var_mean_tNlb ::
  -- | self
  Tensor ->
  -- | dim
  [Dimname] ->
  -- | correction
  Int ->
  -- | keepdim
  Bool ->
  (Tensor, Tensor)
var_mean_tNlb _self _dim _correction _keepdim = unsafePerformIO $ (cast4 ATen.var_mean_tNlb) _self _dim _correction _keepdim

where' ::
  -- | condition
  Tensor ->
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
where' _condition _self _other = unsafePerformIO $ (cast3 ATen.where_ttt) _condition _self _other

where_tst ::
  -- | condition
  Tensor ->
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
where_tst _condition _self _other = unsafePerformIO $ (cast3 ATen.where_tst) _condition _self _other

where_tts ::
  -- | condition
  Tensor ->
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
where_tts _condition _self _other = unsafePerformIO $ (cast3 ATen.where_tts) _condition _self _other

where_tss ::
  -- | condition
  Tensor ->
  -- | self
  Float ->
  -- | other
  Float ->
  Tensor
where_tss _condition _self _other = unsafePerformIO $ (cast3 ATen.where_tss) _condition _self _other

isNonZero ::
  -- | condition
  Tensor ->
  [Tensor]
isNonZero _condition = unsafePerformIO $ (cast1 ATen.where_t) _condition

norm_except_dim ::
  -- | v
  Tensor ->
  -- | pow
  Int ->
  -- | dim
  Int ->
  Tensor
norm_except_dim _v _pow _dim = unsafePerformIO $ (cast3 ATen.norm_except_dim_tll) _v _pow _dim

native_norm_ts ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  Tensor
native_norm_ts _self _p = unsafePerformIO $ (cast2 ATen.native_norm_ts) _self _p

native_norm_tslbs ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
native_norm_tslbs _self _p _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.native_norm_tslbs) _self _p _dim _keepdim _dtype

normCastAll ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dtype
  DType ->
  Tensor
normCastAll _self _p _dtype = unsafePerformIO $ (cast3 ATen.norm_tss) _self _p _dtype

normAll ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  Tensor
normAll _self _p = unsafePerformIO $ (cast2 ATen.norm_ts) _self _p

normCastDim ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
normCastDim _self _p _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.norm_tslbs) _self _p _dim _keepdim _dtype

normDim ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
normDim _self _p _dim _keepdim = unsafePerformIO $ (cast4 ATen.norm_tslb) _self _p _dim _keepdim

norm_tsNbs ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dim
  [Dimname] ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
norm_tsNbs _self _p _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.norm_tsNbs) _self _p _dim _keepdim _dtype

norm_tsNb ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dim
  [Dimname] ->
  -- | keepdim
  Bool ->
  Tensor
norm_tsNb _self _p _dim _keepdim = unsafePerformIO $ (cast4 ATen.norm_tsNb) _self _p _dim _keepdim

frexp ::
  -- | self
  Tensor ->
  (Tensor, Tensor)
frexp _self = unsafePerformIO $ (cast1 ATen.frexp_t) _self

frobeniusNormAll ::
  -- | self
  Tensor ->
  Tensor
frobeniusNormAll _self = unsafePerformIO $ (cast1 ATen.frobenius_norm_t) _self

frobeniusNormDim ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
frobeniusNormDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.frobenius_norm_tlb) _self _dim _keepdim

nuclearNormAll ::
  -- | self
  Tensor ->
  -- | keepdim
  Bool ->
  Tensor
nuclearNormAll _self _keepdim = unsafePerformIO $ (cast2 ATen.nuclear_norm_tb) _self _keepdim

nuclearNormDim ::
  -- | self
  Tensor ->
  -- | dim
  (Int, Int) ->
  -- | keepdim
  Bool ->
  Tensor
nuclearNormDim _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.nuclear_norm_tlb) _self _dim _keepdim

clone ::
  -- | self
  Tensor ->
  -- | memory_format
  ATen.MemoryFormat ->
  Tensor
clone _self _memory_format = unsafePerformIO $ (cast2 ATen.clone_tM) _self _memory_format

positive ::
  -- | self
  Tensor ->
  Tensor
positive _self = unsafePerformIO $ (cast1 ATen.positive_t) _self

sub ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | alpha
  Float ->
  Tensor
sub _self _other _alpha = unsafePerformIO $ (cast3 ATen.sub_tts) _self _other _alpha

subScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  -- | alpha
  Float ->
  Tensor
subScalar _self _other _alpha = unsafePerformIO $ (cast3 ATen.sub_tss) _self _other _alpha

subtract_tts ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | alpha
  Float ->
  Tensor
subtract_tts _self _other _alpha = unsafePerformIO $ (cast3 ATen.subtract_tts) _self _other _alpha

subtract_tss ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  -- | alpha
  Float ->
  Tensor
subtract_tss _self _other _alpha = unsafePerformIO $ (cast3 ATen.subtract_tss) _self _other _alpha

rsub ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | alpha
  Float ->
  Tensor
rsub _self _other _alpha = unsafePerformIO $ (cast3 ATen.rsub_tts) _self _other _alpha

heaviside ::
  -- | self
  Tensor ->
  -- | values
  Tensor ->
  Tensor
heaviside _self _values = unsafePerformIO $ (cast2 ATen.heaviside_tt) _self _values

rsubScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  -- | alpha
  Float ->
  Tensor
rsubScalar _self _other _alpha = unsafePerformIO $ (cast3 ATen.rsub_tss) _self _other _alpha

sparse_sampled_addmm ::
  -- | self
  Tensor ->
  -- | mat1
  Tensor ->
  -- | mat2
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
sparse_sampled_addmm _self _mat1 _mat2 _beta _alpha = unsafePerformIO $ (cast5 ATen.sparse_sampled_addmm_tttss) _self _mat1 _mat2 _beta _alpha

addmm ::
  -- | self
  Tensor ->
  -- | mat1
  Tensor ->
  -- | mat2
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
addmm _self _mat1 _mat2 _beta _alpha = unsafePerformIO $ (cast5 ATen.addmm_tttss) _self _mat1 _mat2 _beta _alpha

hspmm ::
  -- | mat1
  Tensor ->
  -- | mat2
  Tensor ->
  Tensor
hspmm _mat1 _mat2 = unsafePerformIO $ (cast2 ATen.hspmm_tt) _mat1 _mat2

unbind ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  [Tensor]
unbind _self _dim = unsafePerformIO $ (cast2 ATen.unbind_tl) _self _dim

unbindWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  [Tensor]
unbindWithDimname _self _dim = unsafePerformIO $ (cast2 ATen.unbind_tn) _self _dim

mkldnn_reorder_conv2d_weight ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | groups
  Int ->
  Tensor
mkldnn_reorder_conv2d_weight _self _padding _stride _dilation _groups = unsafePerformIO $ (cast5 ATen.mkldnn_reorder_conv2d_weight_tllll) _self _padding _stride _dilation _groups

mkldnn_reorder_conv3d_weight ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int, Int) ->
  -- | stride
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  -- | groups
  Int ->
  Tensor
mkldnn_reorder_conv3d_weight _self _padding _stride _dilation _groups = unsafePerformIO $ (cast5 ATen.mkldnn_reorder_conv3d_weight_tllll) _self _padding _stride _dilation _groups

quantize_per_tensor_dynamic ::
  -- | self
  Tensor ->
  -- | dtype
  DType ->
  -- | reduce_range
  Bool ->
  Tensor
quantize_per_tensor_dynamic _self _dtype _reduce_range = unsafePerformIO $ (cast3 ATen.quantize_per_tensor_dynamic_tsb) _self _dtype _reduce_range

quantize_per_tensor_tdls ::
  -- | self
  Tensor ->
  -- | scale
  Double ->
  -- | zero_point
  Int ->
  -- | dtype
  DType ->
  Tensor
quantize_per_tensor_tdls _self _scale _zero_point _dtype = unsafePerformIO $ (cast4 ATen.quantize_per_tensor_tdls) _self _scale _zero_point _dtype

quantize_per_tensor_ttts ::
  -- | self
  Tensor ->
  -- | scale
  Tensor ->
  -- | zero_point
  Tensor ->
  -- | dtype
  DType ->
  Tensor
quantize_per_tensor_ttts _self _scale _zero_point _dtype = unsafePerformIO $ (cast4 ATen.quantize_per_tensor_ttts) _self _scale _zero_point _dtype

quantize_per_tensor_ltts ::
  -- | tensors
  [Tensor] ->
  -- | scales
  Tensor ->
  -- | zero_points
  Tensor ->
  -- | dtype
  DType ->
  [Tensor]
quantize_per_tensor_ltts _tensors _scales _zero_points _dtype = unsafePerformIO $ (cast4 ATen.quantize_per_tensor_ltts) _tensors _scales _zero_points _dtype

quantize_per_channel ::
  -- | self
  Tensor ->
  -- | scales
  Tensor ->
  -- | zero_points
  Tensor ->
  -- | axis
  Int ->
  -- | dtype
  DType ->
  Tensor
quantize_per_channel _self _scales _zero_points _axis _dtype = unsafePerformIO $ (cast5 ATen.quantize_per_channel_tttls) _self _scales _zero_points _axis _dtype

dequantize_t ::
  -- | self
  Tensor ->
  Tensor
dequantize_t _self = unsafePerformIO $ (cast1 ATen.dequantize_t) _self

dequantize_l ::
  -- | tensors
  [Tensor] ->
  [Tensor]
dequantize_l _tensors = unsafePerformIO $ (cast1 ATen.dequantize_l) _tensors

q_scale ::
  -- | self
  Tensor ->
  Double
q_scale _self = unsafePerformIO $ (cast1 ATen.q_scale_t) _self

q_zero_point ::
  -- | self
  Tensor ->
  Int
q_zero_point _self = unsafePerformIO $ (cast1 ATen.q_zero_point_t) _self

q_per_channel_scales ::
  -- | self
  Tensor ->
  Tensor
q_per_channel_scales _self = unsafePerformIO $ (cast1 ATen.q_per_channel_scales_t) _self

q_per_channel_zero_points ::
  -- | self
  Tensor ->
  Tensor
q_per_channel_zero_points _self = unsafePerformIO $ (cast1 ATen.q_per_channel_zero_points_t) _self

q_per_channel_axis ::
  -- | self
  Tensor ->
  Int
q_per_channel_axis _self = unsafePerformIO $ (cast1 ATen.q_per_channel_axis_t) _self

int_repr ::
  -- | self
  Tensor ->
  Tensor
int_repr _self = unsafePerformIO $ (cast1 ATen.int_repr_t) _self

fake_quantize_per_tensor_affine_tdlll ::
  -- | self
  Tensor ->
  -- | scale
  Double ->
  -- | zero_point
  Int ->
  -- | quant_min
  Int ->
  -- | quant_max
  Int ->
  Tensor
fake_quantize_per_tensor_affine_tdlll _self _scale _zero_point _quant_min _quant_max = unsafePerformIO $ (cast5 ATen.fake_quantize_per_tensor_affine_tdlll) _self _scale _zero_point _quant_min _quant_max

fake_quantize_per_tensor_affine_tttll ::
  -- | self
  Tensor ->
  -- | scale
  Tensor ->
  -- | zero_point
  Tensor ->
  -- | quant_min
  Int ->
  -- | quant_max
  Int ->
  Tensor
fake_quantize_per_tensor_affine_tttll _self _scale _zero_point _quant_min _quant_max = unsafePerformIO $ (cast5 ATen.fake_quantize_per_tensor_affine_tttll) _self _scale _zero_point _quant_min _quant_max

fake_quantize_per_tensor_affine_cachemask ::
  -- | self
  Tensor ->
  -- | scale
  Double ->
  -- | zero_point
  Int ->
  -- | quant_min
  Int ->
  -- | quant_max
  Int ->
  (Tensor, Tensor)
fake_quantize_per_tensor_affine_cachemask _self _scale _zero_point _quant_min _quant_max = unsafePerformIO $ (cast5 ATen.fake_quantize_per_tensor_affine_cachemask_tdlll) _self _scale _zero_point _quant_min _quant_max

fake_quantize_per_channel_affine ::
  -- | self
  Tensor ->
  -- | scale
  Tensor ->
  -- | zero_point
  Tensor ->
  -- | axis
  Int ->
  -- | quant_min
  Int ->
  -- | quant_max
  Int ->
  Tensor
fake_quantize_per_channel_affine _self _scale _zero_point _axis _quant_min _quant_max = unsafePerformIO $ (cast6 ATen.fake_quantize_per_channel_affine_tttlll) _self _scale _zero_point _axis _quant_min _quant_max

fake_quantize_per_channel_affine_cachemask ::
  -- | self
  Tensor ->
  -- | scale
  Tensor ->
  -- | zero_point
  Tensor ->
  -- | axis
  Int ->
  -- | quant_min
  Int ->
  -- | quant_max
  Int ->
  (Tensor, Tensor)
fake_quantize_per_channel_affine_cachemask _self _scale _zero_point _axis _quant_min _quant_max = unsafePerformIO $ (cast6 ATen.fake_quantize_per_channel_affine_cachemask_tttlll) _self _scale _zero_point _axis _quant_min _quant_max

fused_moving_avg_obs_fake_quant ::
  -- | self
  Tensor ->
  -- | observer_on
  Tensor ->
  -- | fake_quant_on
  Tensor ->
  -- | running_min
  Tensor ->
  -- | running_max
  Tensor ->
  -- | scale
  Tensor ->
  -- | zero_point
  Tensor ->
  -- | averaging_const
  Double ->
  -- | quant_min
  Int ->
  -- | quant_max
  Int ->
  -- | ch_axis
  Int ->
  -- | per_row_fake_quant
  Bool ->
  -- | symmetric_quant
  Bool ->
  Tensor
fused_moving_avg_obs_fake_quant _self _observer_on _fake_quant_on _running_min _running_max _scale _zero_point _averaging_const _quant_min _quant_max _ch_axis _per_row_fake_quant _symmetric_quant = unsafePerformIO $ (cast13 ATen.fused_moving_avg_obs_fake_quant_tttttttdlllbb) _self _observer_on _fake_quant_on _running_min _running_max _scale _zero_point _averaging_const _quant_min _quant_max _ch_axis _per_row_fake_quant _symmetric_quant

choose_qparams_optimized ::
  -- | input
  Tensor ->
  -- | numel
  Int ->
  -- | n_bins
  Int ->
  -- | ratio
  Double ->
  -- | bit_width
  Int ->
  (Tensor, Tensor)
choose_qparams_optimized _input _numel _n_bins _ratio _bit_width = unsafePerformIO $ (cast5 ATen.choose_qparams_optimized_tlldl) _input _numel _n_bins _ratio _bit_width

meshgrid_l ::
  -- | tensors
  [Tensor] ->
  [Tensor]
meshgrid_l _tensors = unsafePerformIO $ (cast1 ATen.meshgrid_l) _tensors

meshgrid_ls ::
  -- | tensors
  [Tensor] ->
  -- | indexing
  String ->
  [Tensor]
meshgrid_ls _tensors _indexing = unsafePerformIO $ (cast2 ATen.meshgrid_ls) _tensors _indexing

cartesian_prod ::
  -- | tensors
  [Tensor] ->
  Tensor
cartesian_prod _tensors = unsafePerformIO $ (cast1 ATen.cartesian_prod_l) _tensors

combinations ::
  -- | self
  Tensor ->
  -- | r
  Int ->
  -- | with_replacement
  Bool ->
  Tensor
combinations _self _r _with_replacement = unsafePerformIO $ (cast3 ATen.combinations_tlb) _self _r _with_replacement

resultType ::
  -- | tensor
  Tensor ->
  -- | other
  Tensor ->
  DType
resultType _tensor _other = unsafePerformIO $ (cast2 ATen.result_type_tt) _tensor _other

resultTypeScalar ::
  -- | tensor
  Tensor ->
  -- | other
  Float ->
  DType
resultTypeScalar _tensor _other = unsafePerformIO $ (cast2 ATen.result_type_ts) _tensor _other

resultTypeScalar' ::
  -- | scalar
  Float ->
  -- | tensor
  Tensor ->
  DType
resultTypeScalar' _scalar _tensor = unsafePerformIO $ (cast2 ATen.result_type_st) _scalar _tensor

resultTypeScalars ::
  -- | scalar1
  Float ->
  -- | scalar2
  Float ->
  DType
resultTypeScalars _scalar1 _scalar2 = unsafePerformIO $ (cast2 ATen.result_type_ss) _scalar1 _scalar2

can_cast ::
  -- | from
  DType ->
  -- | to
  DType ->
  Bool
can_cast _from _to = unsafePerformIO $ (cast2 ATen.can_cast_ss) _from _to

promote_types ::
  -- | type1
  DType ->
  -- | type2
  DType ->
  DType
promote_types _type1 _type2 = unsafePerformIO $ (cast2 ATen.promote_types_ss) _type1 _type2

lstm ::
  -- | input
  Tensor ->
  -- | hx
  [Tensor] ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  -- | batch_first
  Bool ->
  (Tensor, Tensor, Tensor)
lstm _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first = unsafePerformIO $ (cast9 ATen.lstm_tllbldbbb) _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first

lstm' ::
  -- | data
  Tensor ->
  -- | batch_sizes
  Tensor ->
  -- | hx
  [Tensor] ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  (Tensor, Tensor, Tensor)
lstm' _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional = unsafePerformIO $ (cast9 ATen.lstm_ttllbldbb) _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional

gru ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  -- | batch_first
  Bool ->
  (Tensor, Tensor)
gru _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first = unsafePerformIO $ (cast9 ATen.gru_ttlbldbbb) _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first

gru' ::
  -- | data
  Tensor ->
  -- | batch_sizes
  Tensor ->
  -- | hx
  Tensor ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  (Tensor, Tensor)
gru' _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional = unsafePerformIO $ (cast9 ATen.gru_tttlbldbb) _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional

rnnTanh ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  -- | batch_first
  Bool ->
  (Tensor, Tensor)
rnnTanh _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first = unsafePerformIO $ (cast9 ATen.rnn_tanh_ttlbldbbb) _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first

rnnTanh' ::
  -- | data
  Tensor ->
  -- | batch_sizes
  Tensor ->
  -- | hx
  Tensor ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  (Tensor, Tensor)
rnnTanh' _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional = unsafePerformIO $ (cast9 ATen.rnn_tanh_tttlbldbb) _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional

rnnRelu ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  -- | batch_first
  Bool ->
  (Tensor, Tensor)
rnnRelu _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first = unsafePerformIO $ (cast9 ATen.rnn_relu_ttlbldbbb) _input _hx _params _has_biases _num_layers _dropout _train _bidirectional _batch_first

rnnRelu' ::
  -- | data
  Tensor ->
  -- | batch_sizes
  Tensor ->
  -- | hx
  Tensor ->
  -- | params
  [Tensor] ->
  -- | has_biases
  Bool ->
  -- | num_layers
  Int ->
  -- | dropout
  Double ->
  -- | train
  Bool ->
  -- | bidirectional
  Bool ->
  (Tensor, Tensor)
rnnRelu' _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional = unsafePerformIO $ (cast9 ATen.rnn_relu_tttlbldbb) _data _batch_sizes _hx _params _has_biases _num_layers _dropout _train _bidirectional

lstm_cell ::
  -- | input
  Tensor ->
  -- | hx
  [Tensor] ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  (Tensor, Tensor)
lstm_cell _input _hx _w_ih _w_hh _b_ih _b_hh = unsafePerformIO $ (cast6 ATen.lstm_cell_tltttt) _input _hx _w_ih _w_hh _b_ih _b_hh

gru_cell ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  Tensor
gru_cell _input _hx _w_ih _w_hh _b_ih _b_hh = unsafePerformIO $ (cast6 ATen.gru_cell_tttttt) _input _hx _w_ih _w_hh _b_ih _b_hh

rnn_tanh_cell ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  Tensor
rnn_tanh_cell _input _hx _w_ih _w_hh _b_ih _b_hh = unsafePerformIO $ (cast6 ATen.rnn_tanh_cell_tttttt) _input _hx _w_ih _w_hh _b_ih _b_hh

rnn_relu_cell ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  Tensor
rnn_relu_cell _input _hx _w_ih _w_hh _b_ih _b_hh = unsafePerformIO $ (cast6 ATen.rnn_relu_cell_tttttt) _input _hx _w_ih _w_hh _b_ih _b_hh

quantized_lstm_cell ::
  -- | input
  Tensor ->
  -- | hx
  [Tensor] ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  -- | packed_ih
  Tensor ->
  -- | packed_hh
  Tensor ->
  -- | col_offsets_ih
  Tensor ->
  -- | col_offsets_hh
  Tensor ->
  -- | scale_ih
  Float ->
  -- | scale_hh
  Float ->
  -- | zero_point_ih
  Float ->
  -- | zero_point_hh
  Float ->
  (Tensor, Tensor)
quantized_lstm_cell _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh = unsafePerformIO $ (cast14 ATen.quantized_lstm_cell_tlttttttttssss) _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh

quantized_gru_cell ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  -- | packed_ih
  Tensor ->
  -- | packed_hh
  Tensor ->
  -- | col_offsets_ih
  Tensor ->
  -- | col_offsets_hh
  Tensor ->
  -- | scale_ih
  Float ->
  -- | scale_hh
  Float ->
  -- | zero_point_ih
  Float ->
  -- | zero_point_hh
  Float ->
  Tensor
quantized_gru_cell _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh = unsafePerformIO $ (cast14 ATen.quantized_gru_cell_ttttttttttssss) _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh

quantized_rnn_relu_cell ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  -- | packed_ih
  Tensor ->
  -- | packed_hh
  Tensor ->
  -- | col_offsets_ih
  Tensor ->
  -- | col_offsets_hh
  Tensor ->
  -- | scale_ih
  Float ->
  -- | scale_hh
  Float ->
  -- | zero_point_ih
  Float ->
  -- | zero_point_hh
  Float ->
  Tensor
quantized_rnn_relu_cell _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh = unsafePerformIO $ (cast14 ATen.quantized_rnn_relu_cell_ttttttttttssss) _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh

quantized_rnn_tanh_cell ::
  -- | input
  Tensor ->
  -- | hx
  Tensor ->
  -- | w_ih
  Tensor ->
  -- | w_hh
  Tensor ->
  -- | b_ih
  Tensor ->
  -- | b_hh
  Tensor ->
  -- | packed_ih
  Tensor ->
  -- | packed_hh
  Tensor ->
  -- | col_offsets_ih
  Tensor ->
  -- | col_offsets_hh
  Tensor ->
  -- | scale_ih
  Float ->
  -- | scale_hh
  Float ->
  -- | zero_point_ih
  Float ->
  -- | zero_point_hh
  Float ->
  Tensor
quantized_rnn_tanh_cell _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh = unsafePerformIO $ (cast14 ATen.quantized_rnn_tanh_cell_ttttttttttssss) _input _hx _w_ih _w_hh _b_ih _b_hh _packed_ih _packed_hh _col_offsets_ih _col_offsets_hh _scale_ih _scale_hh _zero_point_ih _zero_point_hh

maskedFillScalar ::
  -- | self
  Tensor ->
  -- | mask
  Tensor ->
  -- | value
  Float ->
  Tensor
maskedFillScalar _self _mask _value = unsafePerformIO $ (cast3 ATen.masked_fill_tts) _self _mask _value

maskedFill ::
  -- | self
  Tensor ->
  -- | mask
  Tensor ->
  -- | value
  Tensor ->
  Tensor
maskedFill _self _mask _value = unsafePerformIO $ (cast3 ATen.masked_fill_ttt) _self _mask _value

masked_scatter ::
  -- | self
  Tensor ->
  -- | mask
  Tensor ->
  -- | source
  Tensor ->
  Tensor
masked_scatter _self _mask _source = unsafePerformIO $ (cast3 ATen.masked_scatter_ttt) _self _mask _source

put ::
  -- | self
  Tensor ->
  -- | index
  Tensor ->
  -- | source
  Tensor ->
  -- | accumulate
  Bool ->
  Tensor
put _self _index _source _accumulate = unsafePerformIO $ (cast4 ATen.put_tttb) _self _index _source _accumulate

index_add_tltts ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | source
  Tensor ->
  -- | alpha
  Float ->
  Tensor
index_add_tltts _self _dim _index _source _alpha = unsafePerformIO $ (cast5 ATen.index_add_tltts) _self _dim _index _source _alpha

index_add_tntts ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | source
  Tensor ->
  -- | alpha
  Float ->
  Tensor
index_add_tntts _self _dim _index _source _alpha = unsafePerformIO $ (cast5 ATen.index_add_tntts) _self _dim _index _source _alpha

indexFillScalar ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | value
  Float ->
  Tensor
indexFillScalar _self _dim _index _value = unsafePerformIO $ (cast4 ATen.index_fill_tlts) _self _dim _index _value

indexFill ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | value
  Tensor ->
  Tensor
indexFill _self _dim _index _value = unsafePerformIO $ (cast4 ATen.index_fill_tltt) _self _dim _index _value

indexFillScalarWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | value
  Float ->
  Tensor
indexFillScalarWithDimname _self _dim _index _value = unsafePerformIO $ (cast4 ATen.index_fill_tnts) _self _dim _index _value

indexFillWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | value
  Tensor ->
  Tensor
indexFillWithDimname _self _dim _index _value = unsafePerformIO $ (cast4 ATen.index_fill_tntt) _self _dim _index _value

scatter ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | src
  Tensor ->
  Tensor
scatter _self _dim _index _src = unsafePerformIO $ (cast4 ATen.scatter_tltt) _self _dim _index _src

scatterScalar ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | value
  Float ->
  Tensor
scatterScalar _self _dim _index _value = unsafePerformIO $ (cast4 ATen.scatter_tlts) _self _dim _index _value

scatter_tltts ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | src
  Tensor ->
  -- | reduce
  String ->
  Tensor
scatter_tltts _self _dim _index _src _reduce = unsafePerformIO $ (cast5 ATen.scatter_tltts) _self _dim _index _src _reduce

scatter_tltss ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | value
  Float ->
  -- | reduce
  String ->
  Tensor
scatter_tltss _self _dim _index _value _reduce = unsafePerformIO $ (cast5 ATen.scatter_tltss) _self _dim _index _value _reduce

scatterWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | src
  Tensor ->
  Tensor
scatterWithDimname _self _dim _index _src = unsafePerformIO $ (cast4 ATen.scatter_tntt) _self _dim _index _src

scatterScalarWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | value
  Float ->
  Tensor
scatterScalarWithDimname _self _dim _index _value = unsafePerformIO $ (cast4 ATen.scatter_tnts) _self _dim _index _value

scatterAdd ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | src
  Tensor ->
  Tensor
scatterAdd _self _dim _index _src = unsafePerformIO $ (cast4 ATen.scatter_add_tltt) _self _dim _index _src

scatterAddWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | src
  Tensor ->
  Tensor
scatterAddWithDimname _self _dim _index _src = unsafePerformIO $ (cast4 ATen.scatter_add_tntt) _self _dim _index _src

scatter_reduce ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | reduce
  String ->
  -- | output_size
  Int ->
  Tensor
scatter_reduce _self _dim _index _reduce _output_size = unsafePerformIO $ (cast5 ATen.scatter_reduce_tltsl) _self _dim _index _reduce _output_size

bitwise_and_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
bitwise_and_ts _self _other = unsafePerformIO $ (cast2 ATen.bitwise_and_ts) _self _other

bitwise_and_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
bitwise_and_tt _self _other = unsafePerformIO $ (cast2 ATen.bitwise_and_tt) _self _other

bitwise_or_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
bitwise_or_ts _self _other = unsafePerformIO $ (cast2 ATen.bitwise_or_ts) _self _other

bitwise_or_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
bitwise_or_tt _self _other = unsafePerformIO $ (cast2 ATen.bitwise_or_tt) _self _other

bitwiseXorScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
bitwiseXorScalar _self _other = unsafePerformIO $ (cast2 ATen.bitwise_xor_ts) _self _other

bitwiseXor ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
bitwiseXor _self _other = unsafePerformIO $ (cast2 ATen.bitwise_xor_tt) _self _other

bitwise_left_shift_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
bitwise_left_shift_tt _self _other = unsafePerformIO $ (cast2 ATen.bitwise_left_shift_tt) _self _other

bitwise_left_shift_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
bitwise_left_shift_ts _self _other = unsafePerformIO $ (cast2 ATen.bitwise_left_shift_ts) _self _other

bitwise_left_shift_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
bitwise_left_shift_st _self _other = unsafePerformIO $ (cast2 ATen.bitwise_left_shift_st) _self _other

bitwise_right_shift_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
bitwise_right_shift_tt _self _other = unsafePerformIO $ (cast2 ATen.bitwise_right_shift_tt) _self _other

bitwise_right_shift_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
bitwise_right_shift_ts _self _other = unsafePerformIO $ (cast2 ATen.bitwise_right_shift_ts) _self _other

bitwise_right_shift_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
bitwise_right_shift_st _self _other = unsafePerformIO $ (cast2 ATen.bitwise_right_shift_st) _self _other

addbmm ::
  -- | self
  Tensor ->
  -- | batch1
  Tensor ->
  -- | batch2
  Tensor ->
  -- | beta
  Float ->
  -- | alpha
  Float ->
  Tensor
addbmm _self _batch1 _batch2 _beta _alpha = unsafePerformIO $ (cast5 ATen.addbmm_tttss) _self _batch1 _batch2 _beta _alpha

diag ::
  -- | self
  Tensor ->
  -- | diagonal
  Int ->
  Tensor
diag _self _diagonal = unsafePerformIO $ (cast2 ATen.diag_tl) _self _diagonal

cross ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | dim
  Int ->
  Tensor
cross _self _other _dim = unsafePerformIO $ (cast3 ATen.cross_ttl) _self _other _dim

triu ::
  -- | self
  Tensor ->
  -- | diagonal
  Int ->
  Tensor
triu _self _diagonal = unsafePerformIO $ (cast2 ATen.triu_tl) _self _diagonal

tril ::
  -- | self
  Tensor ->
  -- | diagonal
  Int ->
  Tensor
tril _self _diagonal = unsafePerformIO $ (cast2 ATen.tril_tl) _self _diagonal

trace ::
  -- | self
  Tensor ->
  Tensor
trace _self = unsafePerformIO $ (cast1 ATen.trace_t) _self

neScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
neScalar _self _other = unsafePerformIO $ (cast2 ATen.ne_ts) _self _other

ne ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
ne _self _other = unsafePerformIO $ (cast2 ATen.ne_tt) _self _other

not_equal_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
not_equal_ts _self _other = unsafePerformIO $ (cast2 ATen.not_equal_ts) _self _other

not_equal_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
not_equal_tt _self _other = unsafePerformIO $ (cast2 ATen.not_equal_tt) _self _other

eqScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
eqScalar _self _other = unsafePerformIO $ (cast2 ATen.eq_ts) _self _other

eq ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
eq _self _other = unsafePerformIO $ (cast2 ATen.eq_tt) _self _other

geScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
geScalar _self _other = unsafePerformIO $ (cast2 ATen.ge_ts) _self _other

ge ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
ge _self _other = unsafePerformIO $ (cast2 ATen.ge_tt) _self _other

greater_equal_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
greater_equal_ts _self _other = unsafePerformIO $ (cast2 ATen.greater_equal_ts) _self _other

greater_equal_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
greater_equal_tt _self _other = unsafePerformIO $ (cast2 ATen.greater_equal_tt) _self _other

leScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
leScalar _self _other = unsafePerformIO $ (cast2 ATen.le_ts) _self _other

le ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
le _self _other = unsafePerformIO $ (cast2 ATen.le_tt) _self _other

less_equal_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
less_equal_ts _self _other = unsafePerformIO $ (cast2 ATen.less_equal_ts) _self _other

less_equal_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
less_equal_tt _self _other = unsafePerformIO $ (cast2 ATen.less_equal_tt) _self _other

gtScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
gtScalar _self _other = unsafePerformIO $ (cast2 ATen.gt_ts) _self _other

gt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
gt _self _other = unsafePerformIO $ (cast2 ATen.gt_tt) _self _other

greater_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
greater_ts _self _other = unsafePerformIO $ (cast2 ATen.greater_ts) _self _other

greater_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
greater_tt _self _other = unsafePerformIO $ (cast2 ATen.greater_tt) _self _other

ltScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
ltScalar _self _other = unsafePerformIO $ (cast2 ATen.lt_ts) _self _other

lt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
lt _self _other = unsafePerformIO $ (cast2 ATen.lt_tt) _self _other

less_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
less_ts _self _other = unsafePerformIO $ (cast2 ATen.less_ts) _self _other

less_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
less_tt _self _other = unsafePerformIO $ (cast2 ATen.less_tt) _self _other

take ::
  -- | self
  Tensor ->
  -- | index
  Tensor ->
  Tensor
take _self _index = unsafePerformIO $ (cast2 ATen.take_tt) _self _index

take_along_dim ::
  -- | self
  Tensor ->
  -- | indices
  Tensor ->
  -- | dim
  Int ->
  Tensor
take_along_dim _self _indices _dim = unsafePerformIO $ (cast3 ATen.take_along_dim_ttl) _self _indices _dim

indexSelect ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  Tensor
indexSelect _self _dim _index = unsafePerformIO $ (cast3 ATen.index_select_tlt) _self _dim _index

indexSelectWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  Tensor
indexSelectWithDimname _self _dim _index = unsafePerformIO $ (cast3 ATen.index_select_tnt) _self _dim _index

masked_select ::
  -- | self
  Tensor ->
  -- | mask
  Tensor ->
  Tensor
masked_select _self _mask = unsafePerformIO $ (cast2 ATen.masked_select_tt) _self _mask

nonzero ::
  -- | self
  Tensor ->
  Tensor
nonzero _self = unsafePerformIO $ (cast1 ATen.nonzero_t) _self

nonzero_numpy ::
  -- | self
  Tensor ->
  [Tensor]
nonzero_numpy _self = unsafePerformIO $ (cast1 ATen.nonzero_numpy_t) _self

argwhere ::
  -- | self
  Tensor ->
  Tensor
argwhere _self = unsafePerformIO $ (cast1 ATen.argwhere_t) _self

gather ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | index
  Tensor ->
  -- | sparse_grad
  Bool ->
  Tensor
gather _self _dim _index _sparse_grad = unsafePerformIO $ (cast4 ATen.gather_tltb) _self _dim _index _sparse_grad

gatherWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | index
  Tensor ->
  -- | sparse_grad
  Bool ->
  Tensor
gatherWithDimname _self _dim _index _sparse_grad = unsafePerformIO $ (cast4 ATen.gather_tntb) _self _dim _index _sparse_grad

addcmul ::
  -- | self
  Tensor ->
  -- | tensor1
  Tensor ->
  -- | tensor2
  Tensor ->
  -- | value
  Float ->
  Tensor
addcmul _self _tensor1 _tensor2 _value = unsafePerformIO $ (cast4 ATen.addcmul_ttts) _self _tensor1 _tensor2 _value

addcdiv ::
  -- | self
  Tensor ->
  -- | tensor1
  Tensor ->
  -- | tensor2
  Tensor ->
  -- | value
  Float ->
  Tensor
addcdiv _self _tensor1 _tensor2 _value = unsafePerformIO $ (cast4 ATen.addcdiv_ttts) _self _tensor1 _tensor2 _value

cross_entropy_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  -- | ignore_index
  Int ->
  -- | label_smoothing
  Double ->
  Tensor
cross_entropy_loss _self _target _weight _reduction _ignore_index _label_smoothing = unsafePerformIO $ (cast6 ATen.cross_entropy_loss_tttlld) _self _target _weight _reduction _ignore_index _label_smoothing

lstsq ::
  -- | self
  Tensor ->
  -- | A
  Tensor ->
  (Tensor, Tensor)
lstsq _self _A = unsafePerformIO $ (cast2 ATen.lstsq_tt) _self _A

triangular_solve ::
  -- | self
  Tensor ->
  -- | A
  Tensor ->
  -- | upper
  Bool ->
  -- | transpose
  Bool ->
  -- | unitriangular
  Bool ->
  (Tensor, Tensor)
triangular_solve _self _A _upper _transpose _unitriangular = unsafePerformIO $ (cast5 ATen.triangular_solve_ttbbb) _self _A _upper _transpose _unitriangular

linalg_solve_triangular ::
  -- | self
  Tensor ->
  -- | B
  Tensor ->
  -- | upper
  Bool ->
  -- | left
  Bool ->
  -- | unitriangular
  Bool ->
  Tensor
linalg_solve_triangular _self _B _upper _left _unitriangular = unsafePerformIO $ (cast5 ATen.linalg_solve_triangular_ttbbb) _self _B _upper _left _unitriangular

symeig ::
  -- | self
  Tensor ->
  -- | eigenvectors
  Bool ->
  -- | upper
  Bool ->
  (Tensor, Tensor)
symeig _self _eigenvectors _upper = unsafePerformIO $ (cast3 ATen.symeig_tbb) _self _eigenvectors _upper

eig ::
  -- | self
  Tensor ->
  -- | eigenvectors
  Bool ->
  (Tensor, Tensor)
eig _self _eigenvectors = unsafePerformIO $ (cast2 ATen.eig_tb) _self _eigenvectors

svd ::
  -- | self
  Tensor ->
  -- | some
  Bool ->
  -- | compute_uv
  Bool ->
  (Tensor, Tensor, Tensor)
svd _self _some _compute_uv = unsafePerformIO $ (cast3 ATen.svd_tbb) _self _some _compute_uv

swapaxes ::
  -- | self
  Tensor ->
  -- | axis0
  Int ->
  -- | axis1
  Int ->
  Tensor
swapaxes _self _axis0 _axis1 = unsafePerformIO $ (cast3 ATen.swapaxes_tll) _self _axis0 _axis1

swapdims ::
  -- | self
  Tensor ->
  -- | dim0
  Int ->
  -- | dim1
  Int ->
  Tensor
swapdims _self _dim0 _dim1 = unsafePerformIO $ (cast3 ATen.swapdims_tll) _self _dim0 _dim1

cholesky ::
  -- | self
  Tensor ->
  -- | upper
  Bool ->
  Tensor
cholesky _self _upper = unsafePerformIO $ (cast2 ATen.cholesky_tb) _self _upper

cholesky_solve ::
  -- | self
  Tensor ->
  -- | input2
  Tensor ->
  -- | upper
  Bool ->
  Tensor
cholesky_solve _self _input2 _upper = unsafePerformIO $ (cast3 ATen.cholesky_solve_ttb) _self _input2 _upper

solve ::
  -- | self
  Tensor ->
  -- | A
  Tensor ->
  (Tensor, Tensor)
solve _self _A = unsafePerformIO $ (cast2 ATen.solve_tt) _self _A

cholesky_inverse ::
  -- | self
  Tensor ->
  -- | upper
  Bool ->
  Tensor
cholesky_inverse _self _upper = unsafePerformIO $ (cast2 ATen.cholesky_inverse_tb) _self _upper

qr ::
  -- | self
  Tensor ->
  -- | some
  Bool ->
  (Tensor, Tensor)
qr _self _some = unsafePerformIO $ (cast2 ATen.qr_tb) _self _some

geqrf ::
  -- | self
  Tensor ->
  (Tensor, Tensor)
geqrf _self = unsafePerformIO $ (cast1 ATen.geqrf_t) _self

orgqr ::
  -- | self
  Tensor ->
  -- | input2
  Tensor ->
  Tensor
orgqr _self _input2 = unsafePerformIO $ (cast2 ATen.orgqr_tt) _self _input2

ormqr ::
  -- | self
  Tensor ->
  -- | input2
  Tensor ->
  -- | input3
  Tensor ->
  -- | left
  Bool ->
  -- | transpose
  Bool ->
  Tensor
ormqr _self _input2 _input3 _left _transpose = unsafePerformIO $ (cast5 ATen.ormqr_tttbb) _self _input2 _input3 _left _transpose

lu_solve ::
  -- | self
  Tensor ->
  -- | LU_data
  Tensor ->
  -- | LU_pivots
  Tensor ->
  Tensor
lu_solve _self _LU_data _LU_pivots = unsafePerformIO $ (cast3 ATen.lu_solve_ttt) _self _LU_data _LU_pivots

lu_unpack ::
  -- | LU_data
  Tensor ->
  -- | LU_pivots
  Tensor ->
  -- | unpack_data
  Bool ->
  -- | unpack_pivots
  Bool ->
  (Tensor, Tensor, Tensor)
lu_unpack _LU_data _LU_pivots _unpack_data _unpack_pivots = unsafePerformIO $ (cast4 ATen.lu_unpack_ttbb) _LU_data _LU_pivots _unpack_data _unpack_pivots

lgamma ::
  -- | self
  Tensor ->
  Tensor
lgamma _self = unsafePerformIO $ (cast1 ATen.lgamma_t) _self

digamma ::
  -- | self
  Tensor ->
  Tensor
digamma _self = unsafePerformIO $ (cast1 ATen.digamma_t) _self

polygamma ::
  -- | n
  Int ->
  -- | self
  Tensor ->
  Tensor
polygamma _n _self = unsafePerformIO $ (cast2 ATen.polygamma_lt) _n _self

erfinv ::
  -- | self
  Tensor ->
  Tensor
erfinv _self = unsafePerformIO $ (cast1 ATen.erfinv_t) _self

i0 ::
  -- | self
  Tensor ->
  Tensor
i0 _self = unsafePerformIO $ (cast1 ATen.i0_t) _self

sign ::
  -- | self
  Tensor ->
  Tensor
sign _self = unsafePerformIO $ (cast1 ATen.sign_t) _self

signbit ::
  -- | self
  Tensor ->
  Tensor
signbit _self = unsafePerformIO $ (cast1 ATen.signbit_t) _self

dist ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | p
  Float ->
  Tensor
dist _self _other _p = unsafePerformIO $ (cast3 ATen.dist_tts) _self _other _p

atan2 ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
atan2 _self _other = unsafePerformIO $ (cast2 ATen.atan2_tt) _self _other

arctan2 ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
arctan2 _self _other = unsafePerformIO $ (cast2 ATen.arctan2_tt) _self _other

lerpScalar ::
  -- | self
  Tensor ->
  -- | end
  Tensor ->
  -- | weight
  Float ->
  Tensor
lerpScalar _self _end _weight = unsafePerformIO $ (cast3 ATen.lerp_tts) _self _end _weight

lerp ::
  -- | self
  Tensor ->
  -- | end
  Tensor ->
  -- | weight
  Tensor ->
  Tensor
lerp _self _end _weight = unsafePerformIO $ (cast3 ATen.lerp_ttt) _self _end _weight

histc ::
  -- | self
  Tensor ->
  -- | bins
  Int ->
  -- | min
  Float ->
  -- | max
  Float ->
  Tensor
histc _self _bins _min _max = unsafePerformIO $ (cast4 ATen.histc_tlss) _self _bins _min _max

histogram_tttb ::
  -- | self
  Tensor ->
  -- | bins
  Tensor ->
  -- | weight
  Tensor ->
  -- | density
  Bool ->
  (Tensor, Tensor)
histogram_tttb _self _bins _weight _density = unsafePerformIO $ (cast4 ATen.histogram_tttb) _self _bins _weight _density

histogram_tlatb ::
  -- | self
  Tensor ->
  -- | bins
  Int ->
  -- | range
  ([Double]) ->
  -- | weight
  Tensor ->
  -- | density
  Bool ->
  (Tensor, Tensor)
histogram_tlatb _self _bins _range _weight _density = unsafePerformIO $ (cast5 ATen.histogram_tlatb) _self _bins _range _weight _density

fmodScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
fmodScalar _self _other = unsafePerformIO $ (cast2 ATen.fmod_ts) _self _other

fmod ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
fmod _self _other = unsafePerformIO $ (cast2 ATen.fmod_tt) _self _other

hypot ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
hypot _self _other = unsafePerformIO $ (cast2 ATen.hypot_tt) _self _other

igamma ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
igamma _self _other = unsafePerformIO $ (cast2 ATen.igamma_tt) _self _other

igammac ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
igammac _self _other = unsafePerformIO $ (cast2 ATen.igammac_tt) _self _other

nextafter ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
nextafter _self _other = unsafePerformIO $ (cast2 ATen.nextafter_tt) _self _other

remainderScalar ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
remainderScalar _self _other = unsafePerformIO $ (cast2 ATen.remainder_ts) _self _other

remainder ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
remainder _self _other = unsafePerformIO $ (cast2 ATen.remainder_tt) _self _other

remainder_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
remainder_st _self _other = unsafePerformIO $ (cast2 ATen.remainder_st) _self _other

minAll ::
  -- | self
  Tensor ->
  Tensor
minAll _self = unsafePerformIO $ (cast1 ATen.min_t) _self

fmin ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
fmin _self _other = unsafePerformIO $ (cast2 ATen.fmin_tt) _self _other

maxAll ::
  -- | self
  Tensor ->
  Tensor
maxAll _self = unsafePerformIO $ (cast1 ATen.max_t) _self

fmax ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
fmax _self _other = unsafePerformIO $ (cast2 ATen.fmax_tt) _self _other

maximum ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
maximum _self _other = unsafePerformIO $ (cast2 ATen.maximum_tt) _self _other

max ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
max _self _other = unsafePerformIO $ (cast2 ATen.max_tt) _self _other

minimum ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
minimum _self _other = unsafePerformIO $ (cast2 ATen.minimum_tt) _self _other

min ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
min _self _other = unsafePerformIO $ (cast2 ATen.min_tt) _self _other

quantile_ttlbs ::
  -- | self
  Tensor ->
  -- | q
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | interpolation
  String ->
  Tensor
quantile_ttlbs _self _q _dim _keepdim _interpolation = unsafePerformIO $ (cast5 ATen.quantile_ttlbs) _self _q _dim _keepdim _interpolation

quantile_tdlbs ::
  -- | self
  Tensor ->
  -- | q
  Double ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | interpolation
  String ->
  Tensor
quantile_tdlbs _self _q _dim _keepdim _interpolation = unsafePerformIO $ (cast5 ATen.quantile_tdlbs) _self _q _dim _keepdim _interpolation

nanquantile_ttlbs ::
  -- | self
  Tensor ->
  -- | q
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | interpolation
  String ->
  Tensor
nanquantile_ttlbs _self _q _dim _keepdim _interpolation = unsafePerformIO $ (cast5 ATen.nanquantile_ttlbs) _self _q _dim _keepdim _interpolation

nanquantile_tdlbs ::
  -- | self
  Tensor ->
  -- | q
  Double ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | interpolation
  String ->
  Tensor
nanquantile_tdlbs _self _q _dim _keepdim _interpolation = unsafePerformIO $ (cast5 ATen.nanquantile_tdlbs) _self _q _dim _keepdim _interpolation

sort ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | descending
  Bool ->
  (Tensor, Tensor)
sort _self _dim _descending = unsafePerformIO $ (cast3 ATen.sort_tlb) _self _dim _descending

sort_tblb ::
  -- | self
  Tensor ->
  -- | stable
  Bool ->
  -- | dim
  Int ->
  -- | descending
  Bool ->
  (Tensor, Tensor)
sort_tblb _self _stable _dim _descending = unsafePerformIO $ (cast4 ATen.sort_tblb) _self _stable _dim _descending

sortWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | descending
  Bool ->
  (Tensor, Tensor)
sortWithDimname _self _dim _descending = unsafePerformIO $ (cast3 ATen.sort_tnb) _self _dim _descending

sort_tbnb ::
  -- | self
  Tensor ->
  -- | stable
  Bool ->
  -- | dim
  Dimname ->
  -- | descending
  Bool ->
  (Tensor, Tensor)
sort_tbnb _self _stable _dim _descending = unsafePerformIO $ (cast4 ATen.sort_tbnb) _self _stable _dim _descending

msort ::
  -- | self
  Tensor ->
  Tensor
msort _self = unsafePerformIO $ (cast1 ATen.msort_t) _self

argsort ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | descending
  Bool ->
  Tensor
argsort _self _dim _descending = unsafePerformIO $ (cast3 ATen.argsort_tlb) _self _dim _descending

argsortWithDimname ::
  -- | self
  Tensor ->
  -- | dim
  Dimname ->
  -- | descending
  Bool ->
  Tensor
argsortWithDimname _self _dim _descending = unsafePerformIO $ (cast3 ATen.argsort_tnb) _self _dim _descending

topk ::
  -- | self
  Tensor ->
  -- | k
  Int ->
  -- | dim
  Int ->
  -- | largest
  Bool ->
  -- | sorted
  Bool ->
  (Tensor, Tensor)
topk _self _k _dim _largest _sorted = unsafePerformIO $ (cast5 ATen.topk_tllbb) _self _k _dim _largest _sorted

all ::
  -- | self
  Tensor ->
  Tensor
all _self = unsafePerformIO $ (cast1 ATen.all_t) _self

any ::
  -- | self
  Tensor ->
  Tensor
any _self = unsafePerformIO $ (cast1 ATen.any_t) _self

renorm ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  -- | dim
  Int ->
  -- | maxnorm
  Float ->
  Tensor
renorm _self _p _dim _maxnorm = unsafePerformIO $ (cast4 ATen.renorm_tsls) _self _p _dim _maxnorm

equal ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Bool
equal _self _other = unsafePerformIO $ (cast2 ATen.equal_tt) _self _other

pow ::
  -- | self
  Tensor ->
  -- | exponent
  Tensor ->
  Tensor
pow _self _exponent = unsafePerformIO $ (cast2 ATen.pow_tt) _self _exponent

powScalar' ::
  -- | self
  Float ->
  -- | exponent
  Tensor ->
  Tensor
powScalar' _self _exponent = unsafePerformIO $ (cast2 ATen.pow_st) _self _exponent

powScalar ::
  -- | self
  Tensor ->
  -- | exponent
  Float ->
  Tensor
powScalar _self _exponent = unsafePerformIO $ (cast2 ATen.pow_ts) _self _exponent

float_power_tt ::
  -- | self
  Tensor ->
  -- | exponent
  Tensor ->
  Tensor
float_power_tt _self _exponent = unsafePerformIO $ (cast2 ATen.float_power_tt) _self _exponent

float_power_st ::
  -- | self
  Float ->
  -- | exponent
  Tensor ->
  Tensor
float_power_st _self _exponent = unsafePerformIO $ (cast2 ATen.float_power_st) _self _exponent

float_power_ts ::
  -- | self
  Tensor ->
  -- | exponent
  Float ->
  Tensor
float_power_ts _self _exponent = unsafePerformIO $ (cast2 ATen.float_power_ts) _self _exponent

alias ::
  -- | self
  Tensor ->
  Tensor
alias _self = unsafePerformIO $ (cast1 ATen.alias_t) _self

bucketize_ttbb ::
  -- | self
  Tensor ->
  -- | boundaries
  Tensor ->
  -- | out_int32
  Bool ->
  -- | right
  Bool ->
  Tensor
bucketize_ttbb _self _boundaries _out_int32 _right = unsafePerformIO $ (cast4 ATen.bucketize_ttbb) _self _boundaries _out_int32 _right

bucketize_stbb ::
  -- | self
  Float ->
  -- | boundaries
  Tensor ->
  -- | out_int32
  Bool ->
  -- | right
  Bool ->
  Tensor
bucketize_stbb _self _boundaries _out_int32 _right = unsafePerformIO $ (cast4 ATen.bucketize_stbb) _self _boundaries _out_int32 _right

searchsorted_ttbbst ::
  -- | sorted_sequence
  Tensor ->
  -- | self
  Tensor ->
  -- | out_int32
  Bool ->
  -- | right
  Bool ->
  -- | side
  String ->
  -- | sorter
  Tensor ->
  Tensor
searchsorted_ttbbst _sorted_sequence _self _out_int32 _right _side _sorter = unsafePerformIO $ (cast6 ATen.searchsorted_ttbbst) _sorted_sequence _self _out_int32 _right _side _sorter

searchsorted_tsbbst ::
  -- | sorted_sequence
  Tensor ->
  -- | self
  Float ->
  -- | out_int32
  Bool ->
  -- | right
  Bool ->
  -- | side
  String ->
  -- | sorter
  Tensor ->
  Tensor
searchsorted_tsbbst _sorted_sequence _self _out_int32 _right _side _sorter = unsafePerformIO $ (cast6 ATen.searchsorted_tsbbst) _sorted_sequence _self _out_int32 _right _side _sorter

mse_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  Tensor
mse_loss _self _target _reduction = unsafePerformIO $ (cast3 ATen.mse_loss_ttl) _self _target _reduction

l1_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  Tensor
l1_loss _self _target _reduction = unsafePerformIO $ (cast3 ATen.l1_loss_ttl) _self _target _reduction

multi_margin_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | p
  Float ->
  -- | margin
  Float ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  Tensor
multi_margin_loss _self _target _p _margin _weight _reduction = unsafePerformIO $ (cast6 ATen.multi_margin_loss_ttsstl) _self _target _p _margin _weight _reduction

multilabel_margin_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  Tensor
multilabel_margin_loss _self _target _reduction = unsafePerformIO $ (cast3 ATen.multilabel_margin_loss_ttl) _self _target _reduction

multilabel_margin_loss_forward ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  (Tensor, Tensor)
multilabel_margin_loss_forward _self _target _reduction = unsafePerformIO $ (cast3 ATen.multilabel_margin_loss_forward_ttl) _self _target _reduction

nll_loss_nd ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  -- | ignore_index
  Int ->
  Tensor
nll_loss_nd _self _target _weight _reduction _ignore_index = unsafePerformIO $ (cast5 ATen.nll_loss_nd_tttll) _self _target _weight _reduction _ignore_index

nll_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  -- | ignore_index
  Int ->
  Tensor
nll_loss _self _target _weight _reduction _ignore_index = unsafePerformIO $ (cast5 ATen.nll_loss_tttll) _self _target _weight _reduction _ignore_index

nll_loss_forward ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  -- | ignore_index
  Int ->
  (Tensor, Tensor)
nll_loss_forward _self _target _weight _reduction _ignore_index = unsafePerformIO $ (cast5 ATen.nll_loss_forward_tttll) _self _target _weight _reduction _ignore_index

nll_loss2d ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  -- | ignore_index
  Int ->
  Tensor
nll_loss2d _self _target _weight _reduction _ignore_index = unsafePerformIO $ (cast5 ATen.nll_loss2d_tttll) _self _target _weight _reduction _ignore_index

nll_loss2d_forward ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | weight
  Tensor ->
  -- | reduction
  Int ->
  -- | ignore_index
  Int ->
  (Tensor, Tensor)
nll_loss2d_forward _self _target _weight _reduction _ignore_index = unsafePerformIO $ (cast5 ATen.nll_loss2d_forward_tttll) _self _target _weight _reduction _ignore_index

smooth_l1_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  -- | beta
  Double ->
  Tensor
smooth_l1_loss _self _target _reduction _beta = unsafePerformIO $ (cast4 ATen.smooth_l1_loss_ttld) _self _target _reduction _beta

huber_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  -- | delta
  Double ->
  Tensor
huber_loss _self _target _reduction _delta = unsafePerformIO $ (cast4 ATen.huber_loss_ttld) _self _target _reduction _delta

soft_margin_loss ::
  -- | self
  Tensor ->
  -- | target
  Tensor ->
  -- | reduction
  Int ->
  Tensor
soft_margin_loss _self _target _reduction = unsafePerformIO $ (cast3 ATen.soft_margin_loss_ttl) _self _target _reduction

elu ::
  -- | self
  Tensor ->
  -- | alpha
  Float ->
  -- | scale
  Float ->
  -- | input_scale
  Float ->
  Tensor
elu _self _alpha _scale _input_scale = unsafePerformIO $ (cast4 ATen.elu_tsss) _self _alpha _scale _input_scale

glu ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Tensor
glu _self _dim = unsafePerformIO $ (cast2 ATen.glu_tl) _self _dim

hardsigmoid ::
  -- | self
  Tensor ->
  Tensor
hardsigmoid _self = unsafePerformIO $ (cast1 ATen.hardsigmoid_t) _self

hardtanh ::
  -- | self
  Tensor ->
  -- | min_val
  Float ->
  -- | max_val
  Float ->
  Tensor
hardtanh _self _min_val _max_val = unsafePerformIO $ (cast3 ATen.hardtanh_tss) _self _min_val _max_val

hardswish ::
  -- | self
  Tensor ->
  Tensor
hardswish _self = unsafePerformIO $ (cast1 ATen.hardswish_t) _self

leaky_relu ::
  -- | self
  Tensor ->
  -- | negative_slope
  Float ->
  Tensor
leaky_relu _self _negative_slope = unsafePerformIO $ (cast2 ATen.leaky_relu_ts) _self _negative_slope

log_sigmoid ::
  -- | self
  Tensor ->
  Tensor
log_sigmoid _self = unsafePerformIO $ (cast1 ATen.log_sigmoid_t) _self

log_sigmoid_forward ::
  -- | self
  Tensor ->
  (Tensor, Tensor)
log_sigmoid_forward _self = unsafePerformIO $ (cast1 ATen.log_sigmoid_forward_t) _self

softplus ::
  -- | self
  Tensor ->
  -- | beta
  Float ->
  -- | threshold
  Float ->
  Tensor
softplus _self _beta _threshold = unsafePerformIO $ (cast3 ATen.softplus_tss) _self _beta _threshold

softshrink ::
  -- | self
  Tensor ->
  -- | lambd
  Float ->
  Tensor
softshrink _self _lambd = unsafePerformIO $ (cast2 ATen.softshrink_ts) _self _lambd

adaptive_avg_pool2d ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  Tensor
adaptive_avg_pool2d _self _output_size = unsafePerformIO $ (cast2 ATen.adaptive_avg_pool2d_tl) _self _output_size

mkldnn_adaptive_avg_pool2d ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  Tensor
mkldnn_adaptive_avg_pool2d _self _output_size = unsafePerformIO $ (cast2 ATen.mkldnn_adaptive_avg_pool2d_tl) _self _output_size

adaptive_avg_pool3d ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int, Int) ->
  Tensor
adaptive_avg_pool3d _self _output_size = unsafePerformIO $ (cast2 ATen.adaptive_avg_pool3d_tl) _self _output_size

adaptive_max_pool2d ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  (Tensor, Tensor)
adaptive_max_pool2d _self _output_size = unsafePerformIO $ (cast2 ATen.adaptive_max_pool2d_tl) _self _output_size

adaptive_max_pool3d ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int, Int) ->
  (Tensor, Tensor)
adaptive_max_pool3d _self _output_size = unsafePerformIO $ (cast2 ATen.adaptive_max_pool3d_tl) _self _output_size

avg_pool2d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | ceil_mode
  Bool ->
  -- | count_include_pad
  Bool ->
  -- | divisor_override
  Int ->
  Tensor
avg_pool2d _self _kernel_size _stride _padding _ceil_mode _count_include_pad _divisor_override = unsafePerformIO $ (cast7 ATen.avg_pool2d_tlllbbl) _self _kernel_size _stride _padding _ceil_mode _count_include_pad _divisor_override

avg_pool3d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | ceil_mode
  Bool ->
  -- | count_include_pad
  Bool ->
  -- | divisor_override
  Int ->
  Tensor
avg_pool3d _self _kernel_size _stride _padding _ceil_mode _count_include_pad _divisor_override = unsafePerformIO $ (cast7 ATen.avg_pool3d_tlllbbl) _self _kernel_size _stride _padding _ceil_mode _count_include_pad _divisor_override

fractional_max_pool2d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | output_size
  (Int, Int) ->
  -- | random_samples
  Tensor ->
  (Tensor, Tensor)
fractional_max_pool2d _self _kernel_size _output_size _random_samples = unsafePerformIO $ (cast4 ATen.fractional_max_pool2d_tllt) _self _kernel_size _output_size _random_samples

fractional_max_pool3d ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | output_size
  (Int, Int, Int) ->
  -- | random_samples
  Tensor ->
  (Tensor, Tensor)
fractional_max_pool3d _self _kernel_size _output_size _random_samples = unsafePerformIO $ (cast4 ATen.fractional_max_pool3d_tllt) _self _kernel_size _output_size _random_samples

max_pool2d_with_indices ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | ceil_mode
  Bool ->
  (Tensor, Tensor)
max_pool2d_with_indices _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.max_pool2d_with_indices_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

max_pool3d_with_indices ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  -- | ceil_mode
  Bool ->
  (Tensor, Tensor)
max_pool3d_with_indices _self _kernel_size _stride _padding _dilation _ceil_mode = unsafePerformIO $ (cast6 ATen.max_pool3d_with_indices_tllllb) _self _kernel_size _stride _padding _dilation _ceil_mode

max_unpool2d ::
  -- | self
  Tensor ->
  -- | indices
  Tensor ->
  -- | output_size
  (Int, Int) ->
  Tensor
max_unpool2d _self _indices _output_size = unsafePerformIO $ (cast3 ATen.max_unpool2d_ttl) _self _indices _output_size

max_unpool3d ::
  -- | self
  Tensor ->
  -- | indices
  Tensor ->
  -- | output_size
  (Int, Int, Int) ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  Tensor
max_unpool3d _self _indices _output_size _stride _padding = unsafePerformIO $ (cast5 ATen.max_unpool3d_ttlll) _self _indices _output_size _stride _padding

reflection_pad1d ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int) ->
  Tensor
reflection_pad1d _self _padding = unsafePerformIO $ (cast2 ATen.reflection_pad1d_tl) _self _padding

reflection_pad2d ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int, Int, Int) ->
  Tensor
reflection_pad2d _self _padding = unsafePerformIO $ (cast2 ATen.reflection_pad2d_tl) _self _padding

reflection_pad3d ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int, Int, Int, Int, Int) ->
  Tensor
reflection_pad3d _self _padding = unsafePerformIO $ (cast2 ATen.reflection_pad3d_tl) _self _padding

replication_pad1d ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int) ->
  Tensor
replication_pad1d _self _padding = unsafePerformIO $ (cast2 ATen.replication_pad1d_tl) _self _padding

replication_pad2d ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int, Int, Int) ->
  Tensor
replication_pad2d _self _padding = unsafePerformIO $ (cast2 ATen.replication_pad2d_tl) _self _padding

replication_pad3d ::
  -- | self
  Tensor ->
  -- | padding
  (Int, Int, Int, Int, Int, Int) ->
  Tensor
replication_pad3d _self _padding = unsafePerformIO $ (cast2 ATen.replication_pad3d_tl) _self _padding

upsample_linear1d_tlba ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | align_corners
  Bool ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_linear1d_tlba _input _output_size _align_corners _scale_factors = unsafePerformIO $ (cast4 ATen.upsample_linear1d_tlba) _input _output_size _align_corners _scale_factors

upsample_bilinear2d_tlba ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | align_corners
  Bool ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_bilinear2d_tlba _input _output_size _align_corners _scale_factors = unsafePerformIO $ (cast4 ATen.upsample_bilinear2d_tlba) _input _output_size _align_corners _scale_factors

upsample_trilinear3d_tlba ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | align_corners
  Bool ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_trilinear3d_tlba _input _output_size _align_corners _scale_factors = unsafePerformIO $ (cast4 ATen.upsample_trilinear3d_tlba) _input _output_size _align_corners _scale_factors

upsample_bicubic2d_tlba ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | align_corners
  Bool ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_bicubic2d_tlba _input _output_size _align_corners _scale_factors = unsafePerformIO $ (cast4 ATen.upsample_bicubic2d_tlba) _input _output_size _align_corners _scale_factors

upsample_nearest1d_tla ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_nearest1d_tla _input _output_size _scale_factors = unsafePerformIO $ (cast3 ATen.upsample_nearest1d_tla) _input _output_size _scale_factors

upsample_nearest2d_tla ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_nearest2d_tla _input _output_size _scale_factors = unsafePerformIO $ (cast3 ATen.upsample_nearest2d_tla) _input _output_size _scale_factors

upsample_nearest3d_tla ::
  -- | input
  Tensor ->
  -- | output_size
  [Int] ->
  -- | scale_factors
  ([Double]) ->
  Tensor
upsample_nearest3d_tla _input _output_size _scale_factors = unsafePerformIO $ (cast3 ATen.upsample_nearest3d_tla) _input _output_size _scale_factors

-- upsample_linear1d_tlbd
--   :: Tensor -- ^ self
--   -> Int -- ^ output_size
--   -> Bool -- ^ align_corners
--   -> Double -- ^ scales
--   -> Tensor
-- upsample_linear1d_tlbd _self _output_size _align_corners _scales = unsafePerformIO $ (cast4 ATen.upsample_linear1d_tlbd) _self _output_size _align_corners _scales

upsample_bilinear2d_tlbdd ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  -- | align_corners
  Bool ->
  -- | scales_h
  Double ->
  -- | scales_w
  Double ->
  Tensor
upsample_bilinear2d_tlbdd _self _output_size _align_corners _scales_h _scales_w = unsafePerformIO $ (cast5 ATen.upsample_bilinear2d_tlbdd) _self _output_size _align_corners _scales_h _scales_w

upsample_bicubic2d_tlbdd ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  -- | align_corners
  Bool ->
  -- | scales_h
  Double ->
  -- | scales_w
  Double ->
  Tensor
upsample_bicubic2d_tlbdd _self _output_size _align_corners _scales_h _scales_w = unsafePerformIO $ (cast5 ATen.upsample_bicubic2d_tlbdd) _self _output_size _align_corners _scales_h _scales_w

upsample_trilinear3d_tlbddd ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int, Int) ->
  -- | align_corners
  Bool ->
  -- | scales_d
  Double ->
  -- | scales_h
  Double ->
  -- | scales_w
  Double ->
  Tensor
upsample_trilinear3d_tlbddd _self _output_size _align_corners _scales_d _scales_h _scales_w = unsafePerformIO $ (cast6 ATen.upsample_trilinear3d_tlbddd) _self _output_size _align_corners _scales_d _scales_h _scales_w

-- upsample_nearest1d_tld
--   :: Tensor -- ^ self
--   -> Int -- ^ output_size
--   -> Double -- ^ scales
--   -> Tensor
-- upsample_nearest1d_tld _self _output_size _scales = unsafePerformIO $ (cast3 ATen.upsample_nearest1d_tld) _self _output_size _scales

upsample_nearest2d_tldd ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  -- | scales_h
  Double ->
  -- | scales_w
  Double ->
  Tensor
upsample_nearest2d_tldd _self _output_size _scales_h _scales_w = unsafePerformIO $ (cast4 ATen.upsample_nearest2d_tldd) _self _output_size _scales_h _scales_w

upsample_nearest3d_tlddd ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int, Int) ->
  -- | scales_d
  Double ->
  -- | scales_h
  Double ->
  -- | scales_w
  Double ->
  Tensor
upsample_nearest3d_tlddd _self _output_size _scales_d _scales_h _scales_w = unsafePerformIO $ (cast5 ATen.upsample_nearest3d_tlddd) _self _output_size _scales_d _scales_h _scales_w

slow_conv_transpose2d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | output_padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  Tensor
slow_conv_transpose2d _self _weight _kernel_size _bias _stride _padding _output_padding _dilation = unsafePerformIO $ (cast8 ATen.slow_conv_transpose2d_ttltllll) _self _weight _kernel_size _bias _stride _padding _output_padding _dilation

slow_conv_transpose3d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | output_padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  Tensor
slow_conv_transpose3d _self _weight _kernel_size _bias _stride _padding _output_padding _dilation = unsafePerformIO $ (cast8 ATen.slow_conv_transpose3d_ttltllll) _self _weight _kernel_size _bias _stride _padding _output_padding _dilation

thnn_conv2d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  Tensor
thnn_conv2d _self _weight _kernel_size _bias _stride _padding = unsafePerformIO $ (cast6 ATen.thnn_conv2d_ttltll) _self _weight _kernel_size _bias _stride _padding

conv_depthwise3d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  Tensor
conv_depthwise3d _self _weight _kernel_size _bias _stride _padding _dilation = unsafePerformIO $ (cast7 ATen.conv_depthwise3d_ttltlll) _self _weight _kernel_size _bias _stride _padding _dilation

slow_conv3d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  Tensor
slow_conv3d _self _weight _kernel_size _bias _stride _padding = unsafePerformIO $ (cast6 ATen.slow_conv3d_ttltll) _self _weight _kernel_size _bias _stride _padding

slow_conv3d_forward ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  Tensor
slow_conv3d_forward _self _weight _kernel_size _bias _stride _padding = unsafePerformIO $ (cast6 ATen.slow_conv3d_forward_ttltll) _self _weight _kernel_size _bias _stride _padding

slow_conv_dilated2d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  Tensor
slow_conv_dilated2d _self _weight _kernel_size _bias _stride _padding _dilation = unsafePerformIO $ (cast7 ATen.slow_conv_dilated2d_ttltlll) _self _weight _kernel_size _bias _stride _padding _dilation

slow_conv_dilated3d ::
  -- | self
  Tensor ->
  -- | weight
  Tensor ->
  -- | kernel_size
  (Int, Int, Int) ->
  -- | bias
  Tensor ->
  -- | stride
  (Int, Int, Int) ->
  -- | padding
  (Int, Int, Int) ->
  -- | dilation
  (Int, Int, Int) ->
  Tensor
slow_conv_dilated3d _self _weight _kernel_size _bias _stride _padding _dilation = unsafePerformIO $ (cast7 ATen.slow_conv_dilated3d_ttltlll) _self _weight _kernel_size _bias _stride _padding _dilation

col2im ::
  -- | self
  Tensor ->
  -- | output_size
  (Int, Int) ->
  -- | kernel_size
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  Tensor
col2im _self _output_size _kernel_size _dilation _padding _stride = unsafePerformIO $ (cast6 ATen.col2im_tlllll) _self _output_size _kernel_size _dilation _padding _stride

column_stack ::
  -- | tensors
  [Tensor] ->
  Tensor
column_stack _tensors = unsafePerformIO $ (cast1 ATen.column_stack_l) _tensors

im2col ::
  -- | self
  Tensor ->
  -- | kernel_size
  (Int, Int) ->
  -- | dilation
  (Int, Int) ->
  -- | padding
  (Int, Int) ->
  -- | stride
  (Int, Int) ->
  Tensor
im2col _self _kernel_size _dilation _padding _stride = unsafePerformIO $ (cast5 ATen.im2col_tllll) _self _kernel_size _dilation _padding _stride

isfinite ::
  -- | self
  Tensor ->
  Tensor
isfinite _self = unsafePerformIO $ (cast1 ATen.isfinite_t) _self

isinf ::
  -- | self
  Tensor ->
  Tensor
isinf _self = unsafePerformIO $ (cast1 ATen.isinf_t) _self

isposinf ::
  -- | self
  Tensor ->
  Tensor
isposinf _self = unsafePerformIO $ (cast1 ATen.isposinf_t) _self

isneginf ::
  -- | self
  Tensor ->
  Tensor
isneginf _self = unsafePerformIO $ (cast1 ATen.isneginf_t) _self

special_entr ::
  -- | self
  Tensor ->
  Tensor
special_entr _self = unsafePerformIO $ (cast1 ATen.special_entr_t) _self

special_ndtri ::
  -- | self
  Tensor ->
  Tensor
special_ndtri _self = unsafePerformIO $ (cast1 ATen.special_ndtri_t) _self

special_expm1 ::
  -- | self
  Tensor ->
  Tensor
special_expm1 _self = unsafePerformIO $ (cast1 ATen.special_expm1_t) _self

special_exp2 ::
  -- | self
  Tensor ->
  Tensor
special_exp2 _self = unsafePerformIO $ (cast1 ATen.special_exp2_t) _self

special_psi ::
  -- | self
  Tensor ->
  Tensor
special_psi _self = unsafePerformIO $ (cast1 ATen.special_psi_t) _self

special_digamma ::
  -- | self
  Tensor ->
  Tensor
special_digamma _self = unsafePerformIO $ (cast1 ATen.special_digamma_t) _self

special_gammaln ::
  -- | self
  Tensor ->
  Tensor
special_gammaln _self = unsafePerformIO $ (cast1 ATen.special_gammaln_t) _self

special_erf ::
  -- | self
  Tensor ->
  Tensor
special_erf _self = unsafePerformIO $ (cast1 ATen.special_erf_t) _self

special_erfc ::
  -- | self
  Tensor ->
  Tensor
special_erfc _self = unsafePerformIO $ (cast1 ATen.special_erfc_t) _self

special_erfcx ::
  -- | self
  Tensor ->
  Tensor
special_erfcx _self = unsafePerformIO $ (cast1 ATen.special_erfcx_t) _self

special_erfinv ::
  -- | self
  Tensor ->
  Tensor
special_erfinv _self = unsafePerformIO $ (cast1 ATen.special_erfinv_t) _self

special_ndtr ::
  -- | self
  Tensor ->
  Tensor
special_ndtr _self = unsafePerformIO $ (cast1 ATen.special_ndtr_t) _self

special_xlog1py_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
special_xlog1py_tt _self _other = unsafePerformIO $ (cast2 ATen.special_xlog1py_tt) _self _other

special_xlog1py_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
special_xlog1py_st _self _other = unsafePerformIO $ (cast2 ATen.special_xlog1py_st) _self _other

special_xlog1py_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
special_xlog1py_ts _self _other = unsafePerformIO $ (cast2 ATen.special_xlog1py_ts) _self _other

special_xlogy_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
special_xlogy_tt _self _other = unsafePerformIO $ (cast2 ATen.special_xlogy_tt) _self _other

special_xlogy_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
special_xlogy_st _self _other = unsafePerformIO $ (cast2 ATen.special_xlogy_st) _self _other

special_xlogy_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
special_xlogy_ts _self _other = unsafePerformIO $ (cast2 ATen.special_xlogy_ts) _self _other

special_zeta_tt ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
special_zeta_tt _self _other = unsafePerformIO $ (cast2 ATen.special_zeta_tt) _self _other

special_zeta_st ::
  -- | self
  Float ->
  -- | other
  Tensor ->
  Tensor
special_zeta_st _self _other = unsafePerformIO $ (cast2 ATen.special_zeta_st) _self _other

special_zeta_ts ::
  -- | self
  Tensor ->
  -- | other
  Float ->
  Tensor
special_zeta_ts _self _other = unsafePerformIO $ (cast2 ATen.special_zeta_ts) _self _other

special_i0 ::
  -- | self
  Tensor ->
  Tensor
special_i0 _self = unsafePerformIO $ (cast1 ATen.special_i0_t) _self

special_i0e ::
  -- | self
  Tensor ->
  Tensor
special_i0e _self = unsafePerformIO $ (cast1 ATen.special_i0e_t) _self

special_i1 ::
  -- | self
  Tensor ->
  Tensor
special_i1 _self = unsafePerformIO $ (cast1 ATen.special_i1_t) _self

special_i1e ::
  -- | self
  Tensor ->
  Tensor
special_i1e _self = unsafePerformIO $ (cast1 ATen.special_i1e_t) _self

special_logit ::
  -- | self
  Tensor ->
  -- | eps
  Double ->
  Tensor
special_logit _self _eps = unsafePerformIO $ (cast2 ATen.special_logit_td) _self _eps

special_polygamma ::
  -- | n
  Int ->
  -- | self
  Tensor ->
  Tensor
special_polygamma _n _self = unsafePerformIO $ (cast2 ATen.special_polygamma_lt) _n _self

special_logsumexp ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  Tensor
special_logsumexp _self _dim _keepdim = unsafePerformIO $ (cast3 ATen.special_logsumexp_tlb) _self _dim _keepdim

special_expit ::
  -- | self
  Tensor ->
  Tensor
special_expit _self = unsafePerformIO $ (cast1 ATen.special_expit_t) _self

special_sinc ::
  -- | self
  Tensor ->
  Tensor
special_sinc _self = unsafePerformIO $ (cast1 ATen.special_sinc_t) _self

special_round ::
  -- | self
  Tensor ->
  -- | decimals
  Int ->
  Tensor
special_round _self _decimals = unsafePerformIO $ (cast2 ATen.special_round_tl) _self _decimals

special_log1p ::
  -- | self
  Tensor ->
  Tensor
special_log1p _self = unsafePerformIO $ (cast1 ATen.special_log1p_t) _self

special_log_softmax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | dtype
  DType ->
  Tensor
special_log_softmax _self _dim _dtype = unsafePerformIO $ (cast3 ATen.special_log_softmax_tls) _self _dim _dtype

special_gammainc ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
special_gammainc _self _other = unsafePerformIO $ (cast2 ATen.special_gammainc_tt) _self _other

special_gammaincc ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
special_gammaincc _self _other = unsafePerformIO $ (cast2 ATen.special_gammaincc_tt) _self _other

special_multigammaln ::
  -- | self
  Tensor ->
  -- | p
  Int ->
  Tensor
special_multigammaln _self _p = unsafePerformIO $ (cast2 ATen.special_multigammaln_tl) _self _p

special_softmax ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  -- | dtype
  DType ->
  Tensor
special_softmax _self _dim _dtype = unsafePerformIO $ (cast3 ATen.special_softmax_tls) _self _dim _dtype

fft_fft ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_fft _self _n _dim _norm = unsafePerformIO $ (cast4 ATen.fft_fft_tlls) _self _n _dim _norm

fft_ifft ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_ifft _self _n _dim _norm = unsafePerformIO $ (cast4 ATen.fft_ifft_tlls) _self _n _dim _norm

fft_rfft ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_rfft _self _n _dim _norm = unsafePerformIO $ (cast4 ATen.fft_rfft_tlls) _self _n _dim _norm

fft_irfft ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_irfft _self _n _dim _norm = unsafePerformIO $ (cast4 ATen.fft_irfft_tlls) _self _n _dim _norm

fft_hfft ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_hfft _self _n _dim _norm = unsafePerformIO $ (cast4 ATen.fft_hfft_tlls) _self _n _dim _norm

fft_ihfft ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_ihfft _self _n _dim _norm = unsafePerformIO $ (cast4 ATen.fft_ihfft_tlls) _self _n _dim _norm

fft_fft2 ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_fft2 _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_fft2_tlls) _self _s _dim _norm

fft_ifft2 ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_ifft2 _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_ifft2_tlls) _self _s _dim _norm

fft_rfft2 ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_rfft2 _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_rfft2_tlls) _self _s _dim _norm

fft_irfft2 ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_irfft2 _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_irfft2_tlls) _self _s _dim _norm

fft_hfft2 ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_hfft2 _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_hfft2_tlls) _self _s _dim _norm

fft_ihfft2 ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_ihfft2 _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_ihfft2_tlls) _self _s _dim _norm

fft_fftn ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_fftn _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_fftn_tlls) _self _s _dim _norm

fft_ifftn ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_ifftn _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_ifftn_tlls) _self _s _dim _norm

fft_rfftn ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_rfftn _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_rfftn_tlls) _self _s _dim _norm

fft_irfftn ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_irfftn _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_irfftn_tlls) _self _s _dim _norm

fft_hfftn ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_hfftn _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_hfftn_tlls) _self _s _dim _norm

fft_ihfftn ::
  -- | self
  Tensor ->
  -- | s
  Int ->
  -- | dim
  Int ->
  -- | norm
  String ->
  Tensor
fft_ihfftn _self _s _dim _norm = unsafePerformIO $ (cast4 ATen.fft_ihfftn_tlls) _self _s _dim _norm

fft_fftshift ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Tensor
fft_fftshift _self _dim = unsafePerformIO $ (cast2 ATen.fft_fftshift_tl) _self _dim

fft_ifftshift ::
  -- | self
  Tensor ->
  -- | dim
  Int ->
  Tensor
fft_ifftshift _self _dim = unsafePerformIO $ (cast2 ATen.fft_ifftshift_tl) _self _dim

linalg_cholesky_ex ::
  -- | self
  Tensor ->
  -- | upper
  Bool ->
  -- | check_errors
  Bool ->
  (Tensor, Tensor)
linalg_cholesky_ex _self _upper _check_errors = unsafePerformIO $ (cast3 ATen.linalg_cholesky_ex_tbb) _self _upper _check_errors

linalg_cholesky ::
  -- | self
  Tensor ->
  -- | upper
  Bool ->
  Tensor
linalg_cholesky _self _upper = unsafePerformIO $ (cast2 ATen.linalg_cholesky_tb) _self _upper

linalg_cross ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | dim
  Int ->
  Tensor
linalg_cross _self _other _dim = unsafePerformIO $ (cast3 ATen.linalg_cross_ttl) _self _other _dim

linalg_lu_factor ::
  -- | A
  Tensor ->
  -- | pivot
  Bool ->
  (Tensor, Tensor)
linalg_lu_factor _A _pivot = unsafePerformIO $ (cast2 ATen.linalg_lu_factor_tb) _A _pivot

linalg_lu_factor_ex ::
  -- | A
  Tensor ->
  -- | pivot
  Bool ->
  -- | check_errors
  Bool ->
  (Tensor, Tensor, Tensor)
linalg_lu_factor_ex _A _pivot _check_errors = unsafePerformIO $ (cast3 ATen.linalg_lu_factor_ex_tbb) _A _pivot _check_errors

linalg_det ::
  -- | self
  Tensor ->
  Tensor
linalg_det _self = unsafePerformIO $ (cast1 ATen.linalg_det_t) _self

det ::
  -- | self
  Tensor ->
  Tensor
det _self = unsafePerformIO $ (cast1 ATen.det_t) _self

linalg_lstsq ::
  -- | self
  Tensor ->
  -- | b
  Tensor ->
  -- | rcond
  Double ->
  -- | driver
  String ->
  (Tensor, Tensor, Tensor, Tensor)
linalg_lstsq _self _b _rcond _driver = unsafePerformIO $ (cast4 ATen.linalg_lstsq_ttds) _self _b _rcond _driver

linalg_matmul ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
linalg_matmul _self _other = unsafePerformIO $ (cast2 ATen.linalg_matmul_tt) _self _other

linalg_matrix_exp ::
  -- | self
  Tensor ->
  Tensor
linalg_matrix_exp _self = unsafePerformIO $ (cast1 ATen.linalg_matrix_exp_t) _self

linalg_slogdet ::
  -- | self
  Tensor ->
  (Tensor, Tensor)
linalg_slogdet _self = unsafePerformIO $ (cast1 ATen.linalg_slogdet_t) _self

linalg_eig ::
  -- | self
  Tensor ->
  (Tensor, Tensor)
linalg_eig _self = unsafePerformIO $ (cast1 ATen.linalg_eig_t) _self

linalg_eigvals ::
  -- | self
  Tensor ->
  Tensor
linalg_eigvals _self = unsafePerformIO $ (cast1 ATen.linalg_eigvals_t) _self

linalg_eigh ::
  -- | self
  Tensor ->
  -- | UPLO
  String ->
  (Tensor, Tensor)
linalg_eigh _self _UPLO = unsafePerformIO $ (cast2 ATen.linalg_eigh_ts) _self _UPLO

linalg_eigvalsh ::
  -- | self
  Tensor ->
  -- | UPLO
  String ->
  Tensor
linalg_eigvalsh _self _UPLO = unsafePerformIO $ (cast2 ATen.linalg_eigvalsh_ts) _self _UPLO

linalg_householder_product ::
  -- | input
  Tensor ->
  -- | tau
  Tensor ->
  Tensor
linalg_householder_product _input _tau = unsafePerformIO $ (cast2 ATen.linalg_householder_product_tt) _input _tau

linalg_inv_ex ::
  -- | self
  Tensor ->
  -- | check_errors
  Bool ->
  (Tensor, Tensor)
linalg_inv_ex _self _check_errors = unsafePerformIO $ (cast2 ATen.linalg_inv_ex_tb) _self _check_errors

linalg_inv ::
  -- | self
  Tensor ->
  Tensor
linalg_inv _self = unsafePerformIO $ (cast1 ATen.linalg_inv_t) _self

inner ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  Tensor
inner _self _other = unsafePerformIO $ (cast2 ATen.inner_tt) _self _other

outer ::
  -- | self
  Tensor ->
  -- | vec2
  Tensor ->
  Tensor
outer _self _vec2 = unsafePerformIO $ (cast2 ATen.outer_tt) _self _vec2

ger ::
  -- | self
  Tensor ->
  -- | vec2
  Tensor ->
  Tensor
ger _self _vec2 = unsafePerformIO $ (cast2 ATen.ger_tt) _self _vec2

linalg_norm_tslbs ::
  -- | self
  Tensor ->
  -- | ord
  Float ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
linalg_norm_tslbs _self _ord _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.linalg_norm_tslbs) _self _ord _dim _keepdim _dtype

-- linalg_norm_tslbs
--   :: Tensor -- ^ self
--   -> String -- ^ ord
--   -> Int -- ^ dim
--   -> Bool -- ^ keepdim
--   -> DType -- ^ dtype
--   -> Tensor
-- linalg_norm_tslbs _self _ord _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.linalg_norm_tslbs) _self _ord _dim _keepdim _dtype

linalg_vector_norm ::
  -- | self
  Tensor ->
  -- | ord
  Float ->
  -- | dim
  Int ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
linalg_vector_norm _self _ord _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.linalg_vector_norm_tslbs) _self _ord _dim _keepdim _dtype

linalg_matrix_norm_tslbs ::
  -- | self
  Tensor ->
  -- | ord
  Float ->
  -- | dim
  [Int] ->
  -- | keepdim
  Bool ->
  -- | dtype
  DType ->
  Tensor
linalg_matrix_norm_tslbs _self _ord _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.linalg_matrix_norm_tslbs) _self _ord _dim _keepdim _dtype

-- linalg_matrix_norm_tslbs
--   :: Tensor -- ^ self
--   -> String -- ^ ord
--   -> [Int] -- ^ dim
--   -> Bool -- ^ keepdim
--   -> DType -- ^ dtype
--   -> Tensor
-- linalg_matrix_norm_tslbs _self _ord _dim _keepdim _dtype = unsafePerformIO $ (cast5 ATen.linalg_matrix_norm_tslbs) _self _ord _dim _keepdim _dtype

linalg_svd ::
  -- | A
  Tensor ->
  -- | full_matrices
  Bool ->
  (Tensor, Tensor, Tensor)
linalg_svd _A _full_matrices = unsafePerformIO $ (cast2 ATen.linalg_svd_tb) _A _full_matrices

linalg_svdvals ::
  -- | A
  Tensor ->
  Tensor
linalg_svdvals _A = unsafePerformIO $ (cast1 ATen.linalg_svdvals_t) _A

linalg_cond_ts ::
  -- | self
  Tensor ->
  -- | p
  Float ->
  Tensor
linalg_cond_ts _self _p = unsafePerformIO $ (cast2 ATen.linalg_cond_ts) _self _p

-- linalg_cond_ts
--   :: Tensor -- ^ self
--   -> String -- ^ p
--   -> Tensor
-- linalg_cond_ts _self _p = unsafePerformIO $ (cast2 ATen.linalg_cond_ts) _self _p

linalg_pinv_tttb ::
  -- | self
  Tensor ->
  -- | atol
  Tensor ->
  -- | rtol
  Tensor ->
  -- | hermitian
  Bool ->
  Tensor
linalg_pinv_tttb _self _atol _rtol _hermitian = unsafePerformIO $ (cast4 ATen.linalg_pinv_tttb) _self _atol _rtol _hermitian

linalg_pinv_tddb ::
  -- | self
  Tensor ->
  -- | atol
  Double ->
  -- | rtol
  Double ->
  -- | hermitian
  Bool ->
  Tensor
linalg_pinv_tddb _self _atol _rtol _hermitian = unsafePerformIO $ (cast4 ATen.linalg_pinv_tddb) _self _atol _rtol _hermitian

linalg_pinv_tdb ::
  -- | self
  Tensor ->
  -- | rcond
  Double ->
  -- | hermitian
  Bool ->
  Tensor
linalg_pinv_tdb _self _rcond _hermitian = unsafePerformIO $ (cast3 ATen.linalg_pinv_tdb) _self _rcond _hermitian

linalg_pinv_ttb ::
  -- | self
  Tensor ->
  -- | rcond
  Tensor ->
  -- | hermitian
  Bool ->
  Tensor
linalg_pinv_ttb _self _rcond _hermitian = unsafePerformIO $ (cast3 ATen.linalg_pinv_ttb) _self _rcond _hermitian

linalg_solve ::
  -- | input
  Tensor ->
  -- | other
  Tensor ->
  Tensor
linalg_solve _input _other = unsafePerformIO $ (cast2 ATen.linalg_solve_tt) _input _other

linalg_tensorinv ::
  -- | self
  Tensor ->
  -- | ind
  Int ->
  Tensor
linalg_tensorinv _self _ind = unsafePerformIO $ (cast2 ATen.linalg_tensorinv_tl) _self _ind

linalg_tensorsolve ::
  -- | self
  Tensor ->
  -- | other
  Tensor ->
  -- | dims
  [Int] ->
  Tensor
linalg_tensorsolve _self _other _dims = unsafePerformIO $ (cast3 ATen.linalg_tensorsolve_ttl) _self _other _dims

linalg_qr ::
  -- | self
  Tensor ->
  -- | mode
  String ->
  (Tensor, Tensor)
linalg_qr _self _mode = unsafePerformIO $ (cast2 ATen.linalg_qr_ts) _self _mode

linalg_matrix_power ::
  -- | self
  Tensor ->
  -- | n
  Int ->
  Tensor
linalg_matrix_power _self _n = unsafePerformIO $ (cast2 ATen.linalg_matrix_power_tl) _self _n

linalg_matrix_rank_tttb ::
  -- | input
  Tensor ->
  -- | atol
  Tensor ->
  -- | rtol
  Tensor ->
  -- | hermitian
  Bool ->
  Tensor
linalg_matrix_rank_tttb _input _atol _rtol _hermitian = unsafePerformIO $ (cast4 ATen.linalg_matrix_rank_tttb) _input _atol _rtol _hermitian

linalg_matrix_rank_tddb ::
  -- | self
  Tensor ->
  -- | atol
  Double ->
  -- | rtol
  Double ->
  -- | hermitian
  Bool ->
  Tensor
linalg_matrix_rank_tddb _self _atol _rtol _hermitian = unsafePerformIO $ (cast4 ATen.linalg_matrix_rank_tddb) _self _atol _rtol _hermitian

linalg_matrix_rank_tdb ::
  -- | self
  Tensor ->
  -- | tol
  Double ->
  -- | hermitian
  Bool ->
  Tensor
linalg_matrix_rank_tdb _self _tol _hermitian = unsafePerformIO $ (cast3 ATen.linalg_matrix_rank_tdb) _self _tol _hermitian

linalg_matrix_rank_ttb ::
  -- | input
  Tensor ->
  -- | tol
  Tensor ->
  -- | hermitian
  Bool ->
  Tensor
linalg_matrix_rank_ttb _input _tol _hermitian = unsafePerformIO $ (cast3 ATen.linalg_matrix_rank_ttb) _input _tol _hermitian

linalg_multi_dot ::
  -- | tensors
  [Tensor] ->
  Tensor
linalg_multi_dot _tensors = unsafePerformIO $ (cast1 ATen.linalg_multi_dot_l) _tensors

segment_reduce ::
  -- | data
  Tensor ->
  -- | reduce
  String ->
  -- | lengths
  Tensor ->
  -- | indices
  Tensor ->
  -- | axis
  Int ->
  -- | unsafe
  Bool ->
  -- | initial
  Float ->
  Tensor
segment_reduce _data _reduce _lengths _indices _axis _unsafe _initial = unsafePerformIO $ (cast7 ATen.segment_reduce_tsttlbs) _data _reduce _lengths _indices _axis _unsafe _initial

pad_sequence ::
  -- | sequences
  [Tensor] ->
  -- | batch_first
  Bool ->
  -- | padding_value
  Double ->
  Tensor
pad_sequence _sequences _batch_first _padding_value = unsafePerformIO $ (cast3 ATen.pad_sequence_lbd) _sequences _batch_first _padding_value

flatten_dense_tensors ::
  -- | tensors
  [Tensor] ->
  Tensor
flatten_dense_tensors _tensors = unsafePerformIO $ (cast1 ATen.flatten_dense_tensors_l) _tensors

unflatten_dense_tensors ::
  -- | flat
  Tensor ->
  -- | tensors
  [Tensor] ->
  [Tensor]
unflatten_dense_tensors _flat _tensors = unsafePerformIO $ (cast2 ATen.unflatten_dense_tensors_tl) _flat _tensors
